{
  "buildCommand": "cd constructo/mobile && npm install && npx expo export --platform web",
  "outputDirectory": "constructo/mobile/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
