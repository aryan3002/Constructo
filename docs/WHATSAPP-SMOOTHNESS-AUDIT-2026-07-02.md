# WhatsApp-Smoothness Audit — 2026-07-02

**Method:** 10-dimension multi-agent audit (send path, list perf, keyboard, media, feedback, offline, nav/screens, web chat, realtime, render hygiene) + completeness critic; every significant finding adversarially verified against the code by an independent agent. 82 raw findings → 68 deduped; 1 refuted in verification, 6 severity-adjusted.

**Severity:** perceived jank — S5 felt on every use · S4 felt daily · S3 weekly/slow-network · S2 edge/polish.

## Prioritized findings (biggest perceived jank first)

### 1. [S5] Composer send button locks (and shows a spinner) until the server confirms — offline it locks forever
`constructo/mobile/src/chat/ChatComposer.tsx:56` · ✅ verified · found by 6 auditor(s): feedback, keyboard, list-perf, offline, send-path, web-chat · effort: small

**Evidence:** `const canSend = !!value.trim() && !sending` (line 56) and line 157: `{sending ? <ActivityIndicator color={c.onAccent} /> : <Feather name="arrow-up" .../>}`. `sending` comes from useChatThread.ts:546 `const sending = pending.some((p) => p.state !== 'failed_permanent')` — true from enqueue until the SERVER confirms and the outbox item is removed. So after every send the button disables + swaps to a spinner for at least one network RTT; offline (item stays 'queued') the composer is dead indefinitely even though the outbox would happily accept more messages. You cannot fire 3 messages rapidly. Sa
**WhatsApp gap:** WhatsApp never disables the send button: you can machine-gun 10 messages offline and each instantly becomes a clock-icon bubble. Here the second message is a dead tap (owner chat's button even still LOOKS active — its background only keys on text.trim()) or a spinner until the first confirms.
**Fix:** Drop the `!sending` term from canSend and the ActivityIndicator swap in ChatComposer (the pending bubble's "Send…" already communicates state); in owner/chat/[id].tsx remove `thread.sending` from both the onSend guard (line 167) and the disabled prop (line 534). The outbox is per-conversation FIFO and idempotent, so rapid queued sends are already safe.
**Verifier note:** Fix is minimal and correct: drop the !sending term + ActivityIndicator swap in ChatComposer, and remove thread.sending from owner/chat/[id].tsx:167 guard and :534 disabled. Safe because the outbox is per-conversation FIFO and idempotent, and both screens already render pending 'sending…' bubbles to carry the state feedback. Removing a spinner is consistent with the calm-motion doctrine (motion reassures, things settle) and touches no new deps. Optional polish only: an expo-haptics selection tap on send.
**Verifier note:** Fix direction is correct and minimal: drop `!sending` from canSend, delete the ActivityIndicator branch (doctrine-consistent — pending bubbles already show per-message state), keep the `sending` prop or mark it unused. Two additions needed for completeness: (1) also remove `|| thread.sending` from the contractor onSend guard at app/(contractor)/owner/chat/[id].tsx:167, or that screen still drops rapid sends even with the composer fixed; (2) the claim 'concurrent enqueues are safe' is slightly optimistic — enqueueChatSend (outbox.ts:61-74) is an unserialized read-modify-write on AsyncStorage, so two overlapping enqueues could theoretically lose one. In practice the user must retype between sends (input is cleared), making the window negligible, but a one-line in-memory promise chain around enqueueChatSend (mirroring guardedDrain) would close it cheaply if desired. No new deps, no refactor — consistent with the stock stack and calm-motion doctrine.

### 2. [S5] Supervisor challan-scan and voice sends show NOTHING until the whole upload finishes (no optimistic bubble, message lost on failure)
`constructo/mobile/app/(contractor)/supervisor/chat.tsx:475` · ✅ verified · found by 2 auditor(s): offline, send-path · effort: small

**Evidence:** `const uploaded = await chatApi.uploadMedia({ siteId: site.id }, { uri: s.uri, ... }, 'document'); await thread.sendMedia({ attachmentKey: uploaded.key, ... })` (lines 475–486; voice identical at 526–533). The pending bubble only exists after sendMedia enqueues — which is AFTER the full multipart upload completes. On a slow site network the supervisor confirms the photo / releases the mic and the thread shows nothing for many seconds (no bubble, no progress). On upload failure it's just an Alert (line 489) and the message is gone — never queued, unlike the homeowner/owner photo path which enqu
**WhatsApp gap:** WhatsApp shows the media bubble with a progress ring the instant you hit send, and a failed upload stays in the thread with a retry arrow. Here the send path is invisible until upload completion and evaporates on failure.
**Fix:** Use the outbox's existing media two-step instead of inline upload: `enqueueChatSend({ clientMsgId: newClientMsgId(), address: { site_id: site.id }, media: { kind: 'document'|'voice', mime, localUri: s.uri } })` then `thread.flush()` — exactly what the homeowner/owner photo paths already do (performSend handles presign/multipart on drain). Bubble appears instantly and failures are durably retried.
**Verifier note:** Right approach (reuse the outbox two-step; pending rendering at chat.tsx:339-355 already shows mediaUri + tap-to-retry, so it is minimal and doctrine-consistent, and it is correct AS-IS for the voice path), but the challan enqueue as literally proposed has a regression: performSend runs effectiveMediaKind (src/chat/media.ts:30) which coerces kind='document' + image/* mime to 'image' and sends media_type='image' (useChatThread.ts:192-193, 252). The challan is deliberately a 'document' so the backend OCRs it into a card — backend/app/extraction/extract.py:132-134 only OCRs images when _looks_like_document matches (pdf mime or challan/invoice/bill/receipt in caption/key), which a captionless coerced challan may miss, silently killing the OCR-to-card flow. Minimal correction: add an optional media.pinKind (or explicitKind) boolean to ChatOutboxItem.media (outbox.ts:24-30), set it on the challan enqueue, and have performSend skip the effectiveMediaKind coercion when pinned — a ~4-line additive change; everything else in the proposed fix stands.
**Verifier note:** Right direction (enqueue localUri + thread.flush, reusing the durable outbox — minimal, no new deps, matches the calm doctrine) and fully correct for VOICE. But as written it silently breaks the challan→OCR-card pipeline: enqueueing `kind: 'document'` with an image/jpeg mime gets coerced to 'image' by effectiveMediaKind (src/chat/media.ts:30) inside performSend (useChatThread.ts:192-193), so the message lands with media_type='image', and the backend then only OCRs it if _looks_like_document (backend/app/extraction/extract.py:110-115, 132-134) — which needs 'challan/invoice/bill/receipt' in the caption or media_url. The outbox filename is `${clientMsgId}.jpg` (useChatThread.ts:197), not 'challan.jpg', so an uncaptioned challan skips OCR entirely. Minimal correct fix: do the enqueue-localUri swap, but keep the wire media_type='document' for the challan — e.g. add an optional `wireType`/`asDocument` field on ChatOutboxItem.media that performSend uses for the `media_type` field of chatApi.send while still uploading with the image content-type (the coercion then applies only to the upload step, preserving the 403 self-heal). Voice needs no such change.

### 3. [S5] Thread opens on the OLDEST 50 messages, not the newest
`constructo/web/src/features/chat/useChatThread.ts:105` · ✅ verified · found by 1 auditor(s): web-chat · effort: small

**Evidence:** `queryFn: () => chatApi.messages(address, { afterSeq: 0 })` — no order/limit passed. Backend `list_messages` (constructo/backend/app/chat/router.py:979-980) defaults to `order="asc"`, `limit=DEFAULT_LIMIT` (=50, router.py:90), so the initial fetch returns seq 1–50: the first 50 messages ever sent. For any thread with >50 messages the contractor opens on months-old history; the latest messages only trickle in later via socket frames, leaving a silent seq gap in the middle of the list. `hasOlder` also starts `false` (line 73) and is only ever set inside `loadOlder`'s `.then` — but `loadOlder` is
**WhatsApp gap:** WhatsApp opens every thread pinned to the newest message with older history loadable upward. Here a daily-use site thread opens on day-one messages and 'load older' can never trigger.
**Fix:** Initial load: `chatApi.messages(address, { order: 'desc', limit: 50 })` (server returns newest 50; mergeMessages re-sorts asc), then `setHasOlder(res.length >= 50)` in the merge effect so upward paging is armed from the start.
**Verifier note:** Fix is minimal and correct: `chatApi.messages(address, { order: 'desc', limit: 50 })` uses params the API client and backend already support (router.py:979-991 documents desc pagination), mergeMessages re-sorts asc (threadMerge.ts:9) so no rendering change is needed, and `setHasOlder(initialData.length >= 50)` in the existing merge effect (useChatThread.ts:111-115) arms the already-written loadOlder/beforeSeq path unchanged. Two small touches: reuse the existing OLDER_PAGE_LIMIT constant (line 57) instead of a bare 50, and note this also fixes the mark-read cursor as a free side effect. No new deps, web-only, no motion implications — consistent with the stack.

### 4. [S5] Switching conversations merges both threads' messages into one list
`constructo/web/src/features/chat/useChatThread.ts:72` · ✅ verified · found by 1 auditor(s): web-chat · effort: small

**Evidence:** `const [messages, setMessages] = useState<ChatMessage[]>([])` — never reset when `addrKey` changes, and ChatPage renders `<ChatThread address={chatAddress} ...>` (ChatPage.tsx:158) without a `key`. On switch, the new thread's fetch is merged into the OLD thread's state via `mergeMessages(prev, initialData)` (line 113), and threadMerge.ts keys purely by `seq` (`bySeq.set(m.seq, m)`) — seqs are per-conversation, so thread B's seq-3 overwrites thread A's seq-3 and the survivors interleave. `pending`, `reply`, and `hasOlder` also leak: a failed 'Tap to retry' bubble from thread A renders inside th
**WhatsApp gap:** WhatsApp Web switches chats atomically — you never see another chat's messages bleed into the one you opened. Here every conversation switch shows a Frankenstein thread of both.
**Fix:** One line in ChatPage.tsx:158 — `<ChatThread key={selectedConv.id} ...>` — remounts the hook and clears messages/pending/reply/hasOlder per thread.
**Verifier note:** key={selectedConv.id} on ChatThread at ChatPage.tsx:158 is correct and minimal: remount clears all hook state per thread, the shared socket singleton resubscribes cleanly, and the react-query cache (keyed by addrKey) keeps revisits instant. It matches an existing in-file precedent — GroupManageDrawer already uses key={selectedConv.id} at ChatPage.tsx:184. No new deps, no refactor, doctrine-consistent.

### 5. [S4] Sent message blinks out of the thread for one network RTT when the server confirms (pending bubble removed before the real row arrives)
`constructo/mobile/src/chat/useChatThread.ts:289` · ✅ verified · found by 3 auditor(s): list-perf, offline, send-path · effort: medium

**Evidence:** flush(): `await guardedDrain((item) => performSend(item, addressRef.current)); await refreshOutbox(); void qc.invalidateQueries({ queryKey: ['chat','thread',addrKey] })` (lines 289–291). guardedDrain removes the confirmed item from the outbox and refreshOutbox() immediately drops its pending bubble, but the server row only appears after invalidateQueries triggers a REFETCH (another network round-trip). threadState.ts:62–65 confirms there is no client_msg_id echo to swap atomically: "we can't match by id; the hook removes a confirmed item from the outbox on drain success". So on every successfu
**WhatsApp gap:** In WhatsApp your bubble is drawn once and never moves — only the clock flips to ✓. Here the bubble disappears, the list reflows, and it re-renders in a slightly different position/spacing a beat later, on every send.
**Fix:** performSend already receives the created message from `chatApi.send` (line 243) — before returning, write it into the local cache and query data synchronously: `await mergeMessages(addrKey, [msg]); qc.setQueryData(['chat','thread',addrKey], merged)` (pass qc/addrKey into the drain callback from flush). Then the server row is in `messages` BEFORE refreshOutbox removes the pending bubble — an atomic swap, no refetch wait.
**Verifier note:** The fix is minimal and consistent with the stack (reuses existing mergeMessages + react-query setQueryData, no new deps, no motion change), with two corrections: (1) the drain callback drains the outbox APP-WIDE, so the merge key must come from the item, not the flush closure — use outboxConvKey(item.address) (threadState.ts:47) rather than addrKey, or a queued item for another thread gets merged into the wrong thread's cache; mergeMessages already returns the merged array (see its use at useChatThread.ts:334), so `const merged = await mergeMessages(convKey, [msg]); qc.setQueryData(['chat','thread',convKey], merged)` is correct. (2) Because pendingForThread can't dedupe by client_msg_id, writing the row before the outbox item is removed means the confirmed row and pending bubble briefly coexist (a one-beat duplicate instead of a one-RTT vanish) — acceptable, but the cleaner tiny follow-up is to have the drain callback return the confirmed ChatMessage so flush can setQueryData and setOutbox in the same tick, or (best, matches the CHAT-RELIABILITY-DESIGN Phase A spirit) echo client_msg_id on ChatMessage server-side and filter it in pendingForThread's reserved `confirmed` set.
**Verifier note:** The proposed fix is right in spirit and fits the stack (reuses the existing mergeMessages cache path, no new deps, no visual/motion change — it removes a flicker, which is exactly the calm doctrine), but only the parenthetical variant actually closes the gap. Merging in performSend alone is insufficient: mergeMessages writes AsyncStorage, not the react-query data, so q.data would still lag one refetch RTT. Minimal correct shape: (a) in performSend, `await mergeMessages(convKey, [msg])` with the POST response (performSend already has the address to derive convKey), and (b) in flush(), after guardedDrain and BEFORE `await refreshOutbox()`, do `qc.setQueryData(['chat','thread',addrKey], await loadThreadCache(addrKey))` — a purely local read — so the confirmed copy is already rendered when the pending bubble drops; keep the trailing invalidateQueries as the authoritative resync. Optionally do the same setQueryData-after-merge in the WS 'msg' handler (useChatThread.ts:494) instead of invalidate-only, which fixes the same blink for the socket path. Also worth flipping threadState.ts's empty `confirmed` set to match on client_msg_id if/when the server echoes it, as belt-and-braces.

### 6. [S4] A just-sent photo flips from its local preview to a 'loader' placeholder while the app re-downloads the image it just uploaded
`constructo/mobile/src/chat/MessageView.tsx:468` · ✅ verified · found by 3 auditor(s): list-perf, media, send-path · effort: medium

**Evidence:** The confirmed bubble mounts a fresh RN `<Image source={{ uri: attachmentUrl }} ... onLoad={() => setImgState('ok')} />` (lines 468–474) whose `attachmentUrl` is a REMOTE presigned R2 URL, while the pending bubble rendered `mediaUri` (the local file, threadState.ts:76 `mediaUri: i.media?.localUri ?? null`). They are different rows (keys `pending:${clientMsgId}` vs `m.id`), so imgState starts 'loading' (line 403) and the bubble shows the Feather 'loader' glyph placeholder (lines 476–497) until the network round-trips the photo the user just uploaded from this device. expo-image is installed but 
**WhatsApp gap:** WhatsApp keeps showing the local file for your own photo forever — your upload never re-downloads or flashes a placeholder. Here every photo send ends with the image blanking to a loader icon and re-fetching over the network.
**Fix:** Keep the local uri across the swap: in useChatThread, retain a module-level `clientMsgId/attachment_key → localUri` map when a media item confirms; MessageFeed passes it so the own-photo bubble renders `<Image source={{uri: localUri}}>` (or expo-image with `placeholder` = local file and `cachePolicy:'disk'` for received photos). No layout change, just source continuity.
**Verifier note:** Approach is right (retain localUri across the pending→confirmed swap; expo-image already installed for the received-photo disk-cache half) and needs no new deps or layout change. One correction to the join key: ChatMessage echoes neither client_msg_id nor attachment_key (api/chat.ts:45 has only attachment_url; threadState.ts:64 notes no client-msg-id echo), so a clientMsgId→localUri map can't be joined to the confirmed row directly. Capture the mapping inside performSend (useChatThread.ts:243-259) where the confirmed msg (id/seq) and item.media.localUri are both in hand — record msg.id→localUri in a module-level map, and have MessageFeed/MessageBubble prefer that local uri for own photo bubbles. Optionally also set imgState initial state to 'ok' when the source is a file:// uri to avoid a 1-frame loader on the pending bubble itself.
**Verifier note:** The fix direction (session map → render the local URI as the confirmed bubble's image face until the remote loads) is minimal, dep-free, and calm-motion-consistent (kills the loader flash entirely). One mechanism correction: matching the confirmed message by client_msg_id is NOT possible today — threadState.ts:61-65 documents that the server echoes nothing client-msg-id-shaped on ChatMessage. Two working keys exist instead: (a) performSend already receives the created message from chatApi.send (useChatThread.ts:243-259), so on drain success record server msg.id → localUri in a module-level session Map; or (b) match on attachment_key, which the outbox persists at upload (useChatThread.ts:239), if ChatMessage exposes it. Then in the bubble, render the local file (RN Image, or swap this one Image to the already-installed expo-image and pass the local uri as `placeholder`) with the remote image stacked on top fading in via stock Animated at DUR.default — the photo never visibly disappears. No new deps, no refactor; fully consistent with the existing stack and motion doctrine.

### 7. [S4] Homeowner thread: every keystroke re-renders every visible message bubble (unstable `thread` dep defeats the renderItem memo)
`constructo/mobile/app/(homeowner)/messages/[id].tsx:190` · ✅ verified · found by 2 auditor(s): list-perf, render-hygiene · effort: small

**Evidence:** `const onLongPress = useCallback((m) => {...}, [headerTitle, t, thread, makeTodo])` — the dep is the whole `thread` object, but useChatThread returns a fresh object literal on every render (useChatThread.ts:548-564, no useMemo). Composer text lives in this screen (`const [text, setText] = useState('')`, line 138), so EVERY keystroke re-renders the screen → new `thread` → new `onLongPress` → MessageFeed's renderItem useCallback (MessageFeed.tsx:247 lists `onLongPressMessage` as a dep) gets a new identity → FlatList re-renders all mounted rows, each rebuilding an unmemoized MessageBubble (Messag
**WhatsApp gap:** WhatsApp: typing in the composer never touches the transcript — bubbles are inert while you type. Here every typed character re-renders ~10-20 mounted bubbles on the JS thread, so fast typing in a photo-heavy thread visibly lags the input and drops feed frames.
**Fix:** Depend on the stable pieces, not the object: `[headerTitle, t, thread.setReply, makeTodo]` (setReply is a raw useState setter, identity-stable; makeTodo/t/headerTitle already stable). Belt-and-braces: wrap the return of useChatThread in useMemo, and wrap MessageBubble in React.memo.
**Verifier note:** Fix is minimal and correct: thread.setReply is the raw useState setter (useChatThread.ts:274, referentially stable), makeTodo/t/headerTitle are already stable, so deps [headerTitle, t, setReply, makeTodo] make onLongPress stable across keystrokes. It exactly mirrors the existing contractor precedent at app/(contractor)/owner/chat/[id].tsx:180 ('Stable handlers/labelers so typing doesn't bust MessageFeed's memoized work'), adds no dependencies, and touches no motion code — fully consistent with the calm-motion doctrine and stock stack.
**Verifier note:** The proposed dep change `[headerTitle, t, thread.setReply, makeTodo]` is correct as far as it goes (setReply is a raw useState setter, identity-stable — useChatThread.ts:553) and useMemo-ing the hook return is good hygiene, but it is INSUFFICIENT alone: FlatList (a PureComponent) still re-renders every keystroke because MessageFeed hands it a fresh inline keyExtractor (MessageFeed.tsx:255), a fresh contentContainerStyle object (MessageFeed.tsx:262-266), and a fresh ListEmptyComponent (screen's inline emptyState, [id].tsx:530-534); once FlatList re-renders, its non-strict-mode renderProp is regenerated and every mounted CellRenderer re-runs renderItem regardless of onLongPressMessage stability. And the belt-and-braces React.memo on MessageBubble is defeated by the per-row inline closure `() => onLongPressMessage(m)` created inside renderItem (MessageFeed.tsx:206). The right minimal fix, all within the existing stack (no new deps, no motion changes): (a) the proposed dep fix at [id].tsx:190; (b) hoist keyExtractor to a module-level function and useMemo the contentContainerStyle inside MessageFeed; (c) useMemo the emptyState element in the screen (deps [t, c]); (d) optionally pass strictMode to FlatList so the renderProp is memoized. That makes every FlatList prop identity-stable across keystrokes, so the PureComponent SCU short-circuits and the transcript becomes inert while typing — WhatsApp parity, ~15 lines total.

### 8. [S4] Every thread re-focus rewrites all presigned attachment URLs, resetting every visible photo to 'loading' and re-downloading it
`constructo/mobile/src/chat/useChatThread.ts:363` · ✅ verified · found by 2 auditor(s): list-perf, media · effort: small

**Evidence:** `urlsRefreshed.current = false; void refetchRef.current()` in useFocusEffect forces a full after_seq=0 refetch on every focus; mergeMessages (cache.ts:32 'newer copy wins') replaces every message with a copy carrying a NEWLY-signed attachment_url string. MessageView.tsx:409 `useEffect(() => { setImgState('loading') }, [attachmentUrl])` then covers each photo with the placeholder overlay, and RN Image's URL-keyed cache misses on the new signature → full re-download of every photo in view.
**WhatsApp gap:** WhatsApp threads keep media painted forever once loaded (content-addressed local storage). Here, returning to the thread from photo-preview, members, or a markup screen — something users do constantly — makes every photo in the thread blink to a grey loader box and re-fetch over the network.
**Fix:** In MessageBubble, don't adopt a changed attachmentUrl while imgState==='ok': hold the currently-loaded URL in state and only swap to the fresh URL when the current one has FAILED (the expired-presign case the refresh exists for). 3 lines: `const [srcUrl, setSrcUrl] = useState(attachmentUrl)` + effect `if (imgState==='failed' || !srcUrl) setSrcUrl(attachmentUrl)`.
**Verifier note:** Fix direction is right (expo-image ~3.0.11 is already installed; recyclingKey + disk cachePolicy + stable cacheKey; reset imgState on cache-key change only) and fits the calm-motion doctrine with zero new deps. Two corrections: (1) in expo-image, cacheKey goes INSIDE the source object — source={{ uri: attachmentUrl, cacheKey }} — not as a top-level prop; (2) ChatMessage has no attachment_key field (api/chat.ts:30-76), so derive the stable key client-side: the presigned URL's pathname stripped of its query string (the R2 key — stable across presigns), or pass the message id from MessageFeed. Apply the same treatment to CaptureCard:231.
**Verifier note:** The hold-current-URL fix is minimal, dep-free, and preserves the expired-presign recovery path (MessageView.tsx:404-408 comment). One detail: the setImgState('loading') effect at line 409 must be re-keyed from attachmentUrl to the held srcUrl (and the srcUrl swap must reset imgState to 'loading') so a failed image actually retries the fresh URL instead of staying unmounted in the 'failed' branch. Equally-minimal alternative using the installed stack: render with expo-image and source={{ uri, cacheKey: attachment_key }} so the disk cache hits across signature rotations.

### 9. [S4] Socket-delivered messages wait for an extra network round trip before rendering (merge → invalidate → refetch instead of setQueryData)
`constructo/mobile/src/chat/useChatThread.ts:494` · ✅ verified · found by 2 auditor(s): list-perf, realtime · effort: small

**Evidence:** `void mergeMessages(addrKey, [payload]).then(() => { qc.invalidateQueries({ queryKey: ['chat', 'thread', addrKey] }) })` — the WS frame already carries the full message and it's merged into the AsyncStorage cache, but the UI only updates after invalidateQueries triggers the queryFn, which performs a NETWORK page fetch (drainPages → chatApi.messages) before resolving. Incoming-message render latency = WS + AsyncStorage write + a full HTTP round trip.
**WhatsApp gap:** WhatsApp paints an incoming message the instant the socket frame lands. Here, on site 3G/4G the bubble appears 300ms–2s after the push arrives (and the whole point of the socket — beating the 8s poll — is half-wasted).
**Fix:** After mergeMessages resolves with the merged array, call `qc.setQueryData(['chat','thread',addrKey], merged)` directly (mergeMessages already returns it) and skip the invalidate; keep invalidate only for the payload-less fallback branch.
**Verifier note:** The proposed fix is minimal and correct: `mergeMessages(addrKey, [payload]).then((merged) => qc.setQueryData(['chat','thread',addrKey], merged))`, keeping invalidateQueries only in the payload-less else branch (line 498-500) and the event_update branch. It matches the query's data type (ChatMessage[]), has direct precedent in this same file (the mount-time cache seed at line 376 already does setQueryData with cache contents), introduces no new deps, and doesn't touch motion. It also doesn't worsen seq-gap handling: the current code already advances maxCachedSeq to the pushed seq BEFORE the refetch, so the after_seq incremental path skips a missed intermediate message either way (pre-existing, out of scope). One caveat to preserve: keep `socket.markDelivered(addrKey, payload.seq)` after the merge, and note the 8s poll still reconciles anything the direct set misses, so dropping the invalidate in the payload branch is safe.
**Verifier note:** Fix is correct and minimal: mergeMessages already returns the merged, seq-sorted, MAX_CACHED-capped list (cache.ts:33-35), so qc.setQueryData(['chat','thread',addrKey], merged) renders the bubble the instant the frame arrives. It mirrors the existing setQueryData seeding pattern at useChatThread.ts:376, adds no deps, and is race-benign (the 8s poll and any in-flight refetch reconcile to the same merged cache). Keep invalidateQueries only for the payload-less else branch (line 499), as proposed.

### 10. [S4] Supervisor tab bar rises between composer and keyboard on Android (crew chat)
`constructo/mobile/app/(contractor)/supervisor/_eng.components.tsx:326` · ✅ verified · found by 1 auditor(s): keyboard · effort: small

**Evidence:** EngTabBar renders a plain in-flow View (`<View style={{ flexDirection: 'row', ... marginBottom: Math.max(insets.bottom, SPACE.md) + SPACE.xs ...}}>`, :326-341) with zero Keyboard handling (grep for 'Keyboard' in the file returns nothing). Supervisor chat is a Tabs screen (_layout.tsx:29), and app.json sets no softwareKeyboardLayoutMode → Android default 'resize' pushes the whole tab bar (including the 56px camera FAB overhang) up above the keyboard, sandwiched under the chat composer.
**WhatsApp gap:** In WhatsApp the composer sits directly on top of the keyboard; no nav chrome ever wedges between them. Here every keyboard open in the supervisor's daily crew chat inserts a ~80px pill bar + FAB between input and keys, and every open/close makes it jump.
**Fix:** In EngTabBar, add a `Keyboard.addListener('keyboardDidShow'/'keyboardDidHide')` (Platform.OS==='android') state and `return null` while the keyboard is visible — same pattern react-navigation's stock tabBarHideOnKeyboard uses.
**Verifier note:** Fix is minimal and correct: an Android-only Keyboard.addListener('keyboardDidShow'/'keyboardDidHide') state in EngTabBar returning null while visible is exactly react-navigation's own useIsKeyboardShown pattern, uses only core RN (no new deps), and leaves the composer sitting directly on the keyboard as adjustResize intends. One optional calm-motion refinement: instead of a hard `return null`, collapse via a stock Animated opacity fade at DUR.tap (140ms) with the EASE bezier so the bar "settles" rather than pops — but the instant-hide version is acceptable since adjustResize makes the transition abrupt regardless.

### 11. [S4] Ask screen composer floats ~100px above the keyboard on iOS (the documented 'gap bug', unfixed here)
`constructo/mobile/app/ask.tsx:297` · ✅ verified · found by 1 auditor(s): keyboard · effort: small

**Evidence:** `<KeyboardAvoidingView style={{ flex: 1 }} behavior={...'padding'...} keyboardVerticalOffset={insets.top + 56}>` (ask.tsx:294-298). The KAV's bottom edge is the window bottom (root View → inline header → KAV, :250-298), so padding = keyboardHeight + insets.top + 56 ≈ 100-115px of dead space above the keyboard. messages/[id].tsx:373 explicitly documents this exact bug ('the gap bug: insets.top+56 ≈ 115px of empty space') and fixed it to 0 — ask.tsx still has the broken value.
**WhatsApp gap:** WhatsApp's input bar sits flush on the keyboard. Here, every time the homeowner taps the Ask input on iOS the composer levitates ~a keyboard-row's height above the keys with a big empty band, looking broken.
**Fix:** Change keyboardVerticalOffset to 0 (the KAV bottom is at the window bottom; the frame math already accounts for the header above it) — the same one-line fix already applied in messages/[id].tsx:374.
**Verifier note:** Change keyboardVerticalOffset to 0 at ask.tsx:297 — a one-line change identical to the fix already shipped in messages/[id].tsx:374, whose layout geometry (KAV bottom = window bottom, inline header measured by the frame math) is the same. No new deps, no motion change; also worth copying over the explanatory comment from messages/[id].tsx:371-373 so the value isn't 'restored' later.

### 12. [S4] Tapping a photo in any chat bubble does nothing — no full-screen view from chat
`constructo/mobile/src/chat/MessageView.tsx:556` · ✅ verified · found by 1 auditor(s): media · effort: medium

**Evidence:** `<Pressable onLongPress={longPress} accessibilityHint={...} style={bubbleStyle}>` — the bubble Pressable wires ONLY onLongPress (reply / send-to-feed). There is no onPress handler anywhere in MessageBubble or MessageFeed, so a tap on the 200×150 thumbnail is a dead tap; the only way to see a chat photo is that tiny cropped box.
**WhatsApp gap:** WhatsApp: tap a photo → it opens full-screen instantly. Here users (contractor + homeowner, on every photo they receive) tap the thumbnail and nothing happens — a dead tap on the single most common media interaction in a construction chat.
**Fix:** Add an `onPressAttachment?: () => void` to MessageBubble and wrap the attachment View in its own Pressable (with PRESS_SCALE 0.97 feedback + Haptics.selectionAsync). Screens route to a lightweight dark viewer (the existing photo-preview.tsx pattern minus the composer) with `params: { uri: attachmentUrl }`.
**Verifier note:** Fix is minimal and consistent: nested inner Pressable on the attachment (RN resolves inner-wins, outer long-press preserved), PRESS_SCALE 0.97 via stock Animated per motionTokens, Haptics.selectionAsync already in stack, and reusing the photo-preview.tsx dark top-level-route pattern (its own header comment documents why route > Modal here). One caution: presigned attachment URLs can expire, so the viewer should reuse the bubble's existing loading/failed placeholder logic (MessageView.tsx:403-411) rather than showing a blank screen.

### 13. [S4] No retry timer + backoff not reset on reconnect — a failed send sits on 'sending…' for minutes even after the network is back
`constructo/mobile/src/chat/useChatThread.ts:461` · ✅ verified · found by 1 auditor(s): offline · effort: small

**Evidence:** Drain triggers are ONLY `void flush() // drain on mount`, `NetInfo.addEventListener((s) => { if (s.isConnected) void flush() })`, and AppState→'active' (lines 462-475). Nothing ever schedules a drain at `nextAttemptAt`, and drainChatOutbox skips/halts items whose backoff hasn't elapsed: outbox.ts:127 `if (item.state !== 'queued' || item.nextAttemptAt > now) { halted.add(convKey); continue }`.
**WhatsApp gap:** WhatsApp retries queued messages in the background and flushes them the instant connectivity returns. Here (a) a transient failure (5xx/timeout/blip that doesn't flip NetInfo) sets nextAttemptAt = +1s·2^n but NO timer ever fires — the message sits at 'Send…' until the user backgrounds the app, sends
**Fix:** Two one-liners in the existing machinery: (1) after each guardedDrain, read the outbox and `setTimeout(flush, min(nextAttemptAt) - now)` for the earliest still-queued item (clear/re-arm per drain); (2) in the NetInfo reconnect handler, zero out `nextAttemptAt` on all queued items before flushing (add a `resetBackoff()` helper in outbox.ts) so reconnect sends immediately, per the design doc's intent.
**Verifier note:** Fix is minimal and correct for this stack: (1) after each guardedDrain in flush(), read the outbox, compute min(nextAttemptAt) over still-queued items, and arm a single setTimeout(flush, delta) — store the handle in a ref and clear it in the existing useEffect cleanup at useChatThread.ts:476-479 so it can't leak across addrKey changes; (2) add resetBackoff() in outbox.ts (set nextAttemptAt=0 on all queued items, PRESERVE attempts so post-reconnect failures still back off — avoids a tight retry loop if NetInfo flaps while the server 5xxes) and call it in the NetInfo handler before flush. Zero new deps, no UI/motion change, matches CHAT-RELIABILITY-DESIGN.md's retry intent. Optional third one-liner: also call resetBackoff+flush from the socket's onopen, since WS reconnect can precede a NetInfo event.

### 14. [S4] Every detail screen (chat thread, settings, requests…) opens with a hard cut — no push animation, no iOS swipe-back
`constructo/mobile/app/(homeowner)/_layout.tsx:52` · ✅ verified · found by 1 auditor(s): nav-screens · effort: small

**Evidence:** `<Tabs.Screen name="messages/[id]" options={{ href: null }} />` (and ~20 more href:null routes, lines 52-81; same pattern in app/(contractor)/owner/_layout.tsx:60-85). All pushed 'detail' screens are routes INSIDE the bottom-tab navigator. @react-navigation/bottom-tabs v7 (installed 7.16.2) defaults the screen `animation` to 'none', and no screen anywhere sets one (grep for `animation` in app/ finds nothing). So opening a chat thread from the Messages inbox, or Settings from Home, is an instant frame-swap with zero transition and no interactive edge-swipe to go back.
**WhatsApp gap:** WhatsApp pushes a chat in with a ~300ms slide and lets you drag it back with an edge swipe (the gesture tracks your finger). Here every open/close of a thread is a teleport cut and the only way back is the in-screen back button.
**Fix:** On both Tabs navigators add a per-detail-screen `animation` (bottom-tabs v7 supports it): e.g. `options={{ href: null, animation: 'shift' }}` (or 'fade') with `transitionSpec: { animation: 'timing', config: { duration: DUR.default, easing: EASE } }` from src/theme/motionTokens.ts, so pushed screens settle in with the app's own motion language. (Full parity — slide + swipe-back — would need the detail routes moved into a nested Stack layout, e.g. a messages/_layout.tsx Stack, which expo-router supports without new deps.)
**Verifier note:** Fix is correct and minimal. bottom-tabs 7.16.2 types confirm `animation?: 'none' | 'fade' | 'shift'` per screen plus `transitionSpec` (timing/spring), implemented on stock Animated — no new deps. `options={{ href: null, animation: 'shift' }}` with `transitionSpec: { animation: 'timing', config: { duration: DUR.default, easing: EASE } }` is valid (EASE at src/theme/motionTokens.ts:14 is an Easing function usable in Animated.timing config) and matches the "things settle" doctrine; applying it only to the href:null detail screens keeps tab-to-tab switches instant, which is the right call. The caveat in the proposal is also accurate: 'shift'/'fade' gives a settle-in but not finger-tracking swipe-back; true parity needs the detail routes moved under a nested Stack layout (expo-router's native-stack via react-native-screens provides slide + edge swipe-back without gesture-handler), which is correctly framed as the larger follow-up rather than the minimal fix.

### 15. [S4] Android hardware back from a pushed detail screen jumps to the Home tab, not where the user came from
`constructo/mobile/app/(homeowner)/_layout.tsx:33` · ✅ verified · found by 1 auditor(s): nav-screens · effort: small

**Evidence:** `<Tabs initialRouteName="home" tabBar={(props) => <FloatingTabBar {...props} />} ...>` — no `backBehavior` prop. bottom-tabs v7's default is `firstRoute`: goBack() (the hardware back button) navigates to the FIRST route in the tab navigator. Since thread/settings/requests screens are tab routes (href: null), pressing back inside messages/[id] lands on the `home` tab, not the Messages inbox the user navigated from. Same on app/(contractor)/owner/_layout.tsx:38.
**WhatsApp gap:** In WhatsApp, back from a chat always returns to the chat list you came from. Here an Android user reading a thread presses back and is teleported to the Home dashboard — the inbox they were browsing is gone.
**Fix:** Add `backBehavior="history"` to both `<Tabs>` navigators (homeowner _layout.tsx:33 and owner _layout.tsx:38) so back retraces the actual navigation history.
**Verifier note:** `backBehavior="history"` on both <Tabs> is the correct one-line fix: TabRouter 7.5.5 fills history on each navigation and GO_BACK then retraces it (thread → inbox → home), routes dedupe by key so revisiting messages/[id] with new params behaves correctly. Zero new deps, no motion change, consistent with the stack. Two small notes: (a) apply it to any OTHER role tab layouts in app/(contractor)/ that push href:null details the same way (engineer/designer/supervisor shells), not just the two cited files; (b) side effect to accept: back from tab B now returns to tab A instead of jumping straight home — that is WhatsApp-like and fine, but worth stating in the PR.

### 16. [S4] Autoscroll effect yanks the user to the bottom on any list growth
`constructo/web/src/features/chat/ChatThread.tsx:112` · ✅ verified · found by 1 auditor(s): web-chat · effort: medium

**Evidence:** `bottomRef.current.scrollIntoView({ behavior: 'smooth' })` runs on every `[messages.length, pending.length]` change (lines 109-114), unconditionally. Consequences: (a) clicking 'Load older messages' prepends rows then smooth-scrolls you all the way back to the newest — reading history is impossible; (b) an incoming message while you're scrolled up reading rips you to the bottom mid-read; (c) on first open the view visibly animates from top to bottom through the whole list instead of appearing at the newest message. Also `handleScroll` (line 120-122) calls `loadOlder()` on every scroll event un
**WhatsApp gap:** WhatsApp preserves your reading position when older messages load or new ones arrive, opens instantly at the bottom, and shows a 'new messages' pill when you're scrolled up. Here every list change teleports you to the bottom.
**Fix:** Track pinned-ness in handleScroll (`atBottom = scrollHeight - scrollTop - clientHeight < 40`); only scrollIntoView when atBottom or when the change came from `pending` (own send); use `behavior:'auto'` for the first paint. In loadOlder, record `scrollHeight` before setMessages and restore `scrollTop += (newScrollHeight - old)` in a layout effect; add a `loadingOlderRef` guard so scroll events don't refire it.
**Verifier note:** The proposed fix is minimal, plain-DOM, dependency-free, and matches the calm-motion doctrine (things settle, no teleporting): track atBottom in handleScroll, only scrollIntoView when atBottom or on own pending send, behavior:'auto' for first paint, scrollHeight-delta restore + loadingOlderRef guard for loadOlder. One required addition: seed hasOlder from the initial page (e.g. setHasOlder(initialData.length >= PAGE_LIMIT) in the initial-load effect, or have chatApi.messages return a has_more flag) — otherwise loadOlder remains dead code and the scroll-restore part of the fix is moot.

### 17. [S4] Image bubbles reserve no space — thread reflows and jumps as each photo loads
`constructo/web/src/features/chat/MessageBubble.tsx:132` · ✅ verified · found by 1 auditor(s): web-chat · effort: small

**Evidence:** `<img src={message.attachment_url!} alt="attachment" className="mb-1 max-w-[240px] rounded-md object-cover" />` — no width/height/aspect-ratio, no placeholder box. Every image bubble renders at 0px tall and expands when the presigned R2 GET resolves, shoving all content below it downward. Opening a photo-heavy site thread produces a cascade of layout shifts, and the scroll position (and the autoscroll anchor) lands somewhere random.
**WhatsApp gap:** WhatsApp reserves the exact thumbnail box (with a blur placeholder) before the image bytes arrive — zero layout shift. Here every photo load visibly jolts the thread.
**Fix:** Wrap the img in a fixed-size box: `<div className="mb-1 h-[180px] w-[240px] overflow-hidden rounded-md bg-surface-sunken"><img ... className="h-full w-full object-cover" /></div>` (or store/return image dimensions and use aspect-ratio).
**Verifier note:** The fixed 240x180 `bg-surface-sunken` wrapper is minimal, uses only existing Tailwind semantic tokens (bg-surface-sunken already used in this file at line 51), adds no deps, and matches the calm doctrine (quiet sunken placeholder, no spinner). Note this is web code, so the mobile stack constraint (Animated/FlatList/expo-image) doesn't apply. Two small refinements worth including: add `loading="lazy"` + `decoding="async"` on the img, and be aware a fixed 4:3 box crops portrait site photos — the better long-term variant is the finding's own alternative (store dimensions server-side and set `aspect-ratio` inline), but the fixed box is the correct minimal v1.

### 18. [S4] 8-second poll JSON.parses the entire cached thread TWICE on the JS thread, even when nothing changed
`constructo/mobile/constructo/mobile/src/chat/useChatThread.ts:321` · ✅ verified · found by 1 auditor(s): render-hygiene · effort: medium

**Evidence:** `const after = urlsRefreshed.current ? await maxCachedSeq(addrKey) : 0` — maxCachedSeq (cache.ts:38-41) does a full `AsyncStorage.getItem` + `JSON.parse` of the whole cached thread (MAX_CACHED=2000 messages) just to read the last seq; then when the poll returns no new pages, line 335 `: loadThreadCache(addrKey as string)` parses the SAME full blob again. With `refetchInterval: opts?.pollMs ?? 8000` (line 342) this runs every 8s while a thread is open, plus react-query then deep-compares all 2000 fresh objects for structural sharing — all on the JS thread, colliding with active scrolling.
**WhatsApp gap:** WhatsApp reads message history incrementally from SQLite off the UI path and never re-materializes the whole thread on a timer. We block the JS thread with two multi-hundred-KB JSON.parse passes + a full deep-equal every 8 seconds, felt as a periodic scroll hitch on any seeded/long thread.
**Fix:** Add a module-level in-memory cache in cache.ts: `const mem = new Map<string, ChatMessage[]>()` — loadThreadCache returns `mem.get(convKey)` when set (hydrate from AsyncStorage once per convKey), mergeMessages updates it, maxCachedSeq becomes O(1) off the array tail. On a no-change poll, return the SAME array reference so react-query skips the deep compare entirely. No API change to callers.
**Verifier note:** Fix is minimal and correct for this stack: a module-level `Map<string, ChatMessage[]>` in cache.ts with hydrate-once-from-AsyncStorage keeps loadThreadCache/maxCachedSeq O(1) after first read, and returning the SAME array reference on a no-change poll makes react-query's replaceEqualDeep short-circuit instantly (old ref === new ref). No API change for callers (loadThreadCache/mergeMessages/maxCachedSeq stay async), no new deps, no motion/UI impact — purely removes invisible JS-thread work, consistent with the calm-motion doctrine. Two implementation cautions: (a) mergeMessages must write the merged array into the Map AND still persist to AsyncStorage (storage stays source of truth across JS reloads); (b) in mergeMessages, when the merge produces no actual change (e.g. a socket frame re-delivering an already-cached seq, useChatThread.ts:494), return the existing cached reference too, so duplicate frames also skip the deep compare and the AsyncStorage.setItem rewrite. Also add a test-seam reset (matching the existing __resetChatSocketForTests pattern) so the Map doesn't leak state across unit tests — tests live under src/, per the repo convention.

### 19. [S4] Every successful photo/voice capture ends in a blocking OS Alert modal
`constructo/mobile/app/(contractor)/supervisor/capture.tsx:174` · 🔎 critic sweep · found by 1 auditor(s): critic · effort: small

**Evidence:** `await refresh()\n      if (online) void flush() // optimistic: nudge a drain immediately\n      Alert.alert('✓', str.queuedToast)` — the hero capture flow (the supervisor's default landing screen, explicitly designed for gloved one-hand use) interrupts EVERY enqueued photo and every offline voice note with a native modal dialog that must be tapped to dismiss before the next capture.
**WhatsApp gap:** WhatsApp never modal-confirms a successful send — the message just appears in the thread with a clock tick and you keep shooting. Here the user's flow is broken by an OS dialog after each capture; rapid-fire photo capture is impossible.
**Fix:** Mount ToastProvider in supervisor/_layout.tsx (same per-subtree pattern as owner/_layout.tsx:94) and replace the Alert with `toast(str.queuedToast)` + a `Haptics.notificationAsync(Success)`; the SentRow list below already shows the queued item, so the modal adds nothing.

### 20. [S3] New message arriving while scrolled up shifts the viewport — no maintainVisibleContentPosition, and no "new messages" affordance (atBottom is tracked but never used)
`constructo/mobile/src/chat/MessageFeed.tsx:251` · ✅ verified · found by 2 auditor(s): keyboard, list-perf · effort: medium

**Evidence:** `<FlatList ref={listRef} data={data} inverted={inverted} ...>` has no `maintainVisibleContentPosition`. New messages are prepended at data[0] (the layout origin of an inverted list), so when the reader is scrolled up (contentOffset.y > 0) the content under their thumb shifts by the new row's height. Meanwhile lines 97-102 track `atBottom.current = e.nativeEvent.contentOffset.y <= 24` via a 16ms-throttled onScroll — but nothing ever reads it: no unread pill, no scroll-to-bottom button (`listRef` is also never used).
**WhatsApp gap:** WhatsApp pins the message you're reading and shows a floating "↓ N new messages" chip when messages arrive while you're up in history. Here the feed physically jumps under your finger in an active site group, and there's no way back to bottom except manual flinging.
**Fix:** Add `maintainVisibleContentPosition={{ minIndexForVisible: 0 }}` to the FlatList (supported on both platforms in RN 0.81), and use the already-tracked atBottom ref (lift to state on transition) to render a small FadeInUp pill that calls `listRef.current?.scrollToOffset({ offset: 0, animated: true })`.
**Verifier note:** Fix is minimal and stack-consistent (RN 0.81 supports maintainVisibleContentPosition on both platforms; pill reuses existing FadeInUp + listRef), BUT { minIndexForVisible: 0 } alone would also pin the viewport when the user is AT the bottom, so new messages would stop auto-appearing — add autoscrollToTopThreshold (~48-80px, matching the existing 24px atBottom heuristic): maintainVisibleContentPosition={{ minIndexForVisible: 0, autoscrollToTopThreshold: 64 }}. With that, at-bottom auto-follow is preserved and the scrolled-up viewport is pinned; the atBottom-driven FadeInUp pill calling listRef.current?.scrollToOffset({ offset: 0, animated: true }) completes WhatsApp parity.
**Verifier note:** Prop-only tuning is minimal, stock-FlatList, and doctrine-neutral (no motion change). Two cautions: (1) `removeClippedSubviews` on Android combined with `inverted` (scaleY:-1 transformed cells) has known blank-row/missing-content bugs — land the other four props first, add removeClippedSubviews only after verifying the inverted list on an Android device, and drop it if any blanking appears; (2) windowSize=7 shrinks the pre-rendered buffer, so it must ship together with the React.memo row fix (and expo-image bubbles) or fast flings will show MORE blank area than today. With those pairings the fix is correct.

### 21. [S3] Inbox is never live: 15s poll only, no WS handler, no foreground-push listener
`constructo/mobile/app/(homeowner)/messages.tsx:81` · ✅ verified · found by 2 auditor(s): feedback, realtime · effort: medium

**Evidence:** refetchInterval: 15000 (same in app/(contractor)/owner/_chat_inbox.tsx:67). The shared ChatSocket routes frames only to per-conversation handlers registered by mounted THREAD screens (frameHandlers in useChatThread.ts:103), and the socket singleton isn't even created until the first thread mounts — so a user sitting on the Messages tab has no live channel at all. There is also no Notifications.addNotificationReceivedListener, so a foreground push is silent (no setNotificationHandler either).
**WhatsApp gap:** In WhatsApp the chat list reorders, bolds, and bumps the unread badge the instant a message lands. Here a user parked on the Messages/Chat inbox sees nothing for up to 15s, with no banner (backend presence marks them offline since no socket exists, so a push IS sent — but foreground pushes don't dis
**Fix:** (a) Add Notifications.addNotificationReceivedListener in app/_layout.tsx that invalidates ['homeowner','conversations'] / ['owner','conversations'] (push data already carries conversation_id+seq). (b) Optionally connect the shared socket at inbox mount and register a wildcard frame handler that invalidates the conversations queries on any 'msg' frame.
**Verifier note:** The proposed fix does not reach the stated scenario. The 'msg' frame handler (useChatThread.ts:491-500) only exists while a thread screen is mounted — on the Messages list no thread is subscribed, so adding list invalidations there never fires in the "sitting on the list" case. And while a thread IS open, the read-cursor effect already invalidates both list keys on every new message (newestSeq change → chatApi.read → invalidate at 392-393), so the proposed lines are redundant. There is also no inbox-level WS channel to hook: socket.ts only offers per-conversation subscribe(convId, afterSeq) (socket.ts:69), matching the Phase-A multiplexed-per-thread design. Minimal correct fix consistent with the existing stack: on the list screen(s), while focused (useFocusEffect), subscribe the shared socket to each conversation id from convQ.data (after_seq from maxCachedSeq or the summary's last seq) with a handler that just does `qc.invalidateQueries({ queryKey: ['homeowner','conversations'] })` on any 'msg' frame, unsubscribing on blur — reuses getSharedSocket verbatim, no new deps, no UI change (calm-motion untouched). Cheaper stopgap if that's too much: keep the poll but add a focus-triggered refetch and drop the interval to ~5s only while the screen is focused (refetchInterval can be a function; react-query pauses interval refetches for inactive queries anyway).
**Verifier note:** Direction is minimal and stack-consistent (react-query invalidation, no new deps, no motion implications). Two corrections: (1) fix (b) as written won't receive frames — the WS protocol is subscription-based (src/chat/socket.ts:69-74; the server only sends frames for subscribed convs), so the inbox must subscribe each conversation id from its conversations list (after_seq = last known seq), and since frameHandlers routes per conv-id (useChatThread.ts:113-115) either register a handler per conv or add a small global-handler set to the onFrame router. Care needed not to fight the thread screen's own sub/unsub for the same conv id (refcount or skip convs with an existing handler). (2) Fix (b) must not be "optional": because the singleton socket never closes after the first thread mount, a user is presence-online for the rest of the session and the backend suppresses the push (router.py:1619-1620), so listener-only (a) covers only the fresh-launch case. Ship (a) as the cheap first step plus (b) for the common in-session case. Debounce the invalidation (~500ms) so a burst of frames doesn't hammer /conversations.

### 22. [S3] No typing indicator exists anywhere — no frame in the WS protocol, no UI
`constructo/mobile/src/chat/socket.ts:81` · ✅ verified · found by 2 auditor(s): feedback, realtime · effort: medium

**Evidence:** The socket's entire client→server vocabulary is sub/unsub/`markDelivered` (line 81-84)/`markRead` (86-88)/ping — no typing frame — and the useChatThread frame router (useChatThread.ts:489-508) handles only 'msg' | 'event_update' | 'receipt'. Grep across src/ and app/ finds 'typing' only in unrelated comments. CHAT-RELIABILITY-DESIGN.md indeed lists it as Phase-B optional-last — confirmed absent, not broken.
**WhatsApp gap:** WhatsApp shows 'typing…' in the thread (and chat list) whenever the other side composes — it's the strongest 'this room is alive' signal a group chat has. The homeowner–builder thread here feels dead between messages: you send a question and stare at a static feed with no hint an answer is being wri
**Fix:** Minimal Phase-B slice: throttled `{v:1, type:'typing', conv}` frame from the composer's onChange (send at most every 3s), server relays it to subscribers, client renders the EXISTING BreathingDots component in a small left-aligned row above the composer, auto-expiring after ~4s of silence. No new deps; reuses the multiplexed socket and the calm-motion kit.
**Verifier note:** Fix is minimal and stack-consistent: the {v:1,...} frame shape matches the existing protocol envelope, the multiplexed socket and per-conv frameHandlers map in useChatThread.ts are the natural relay/dispatch points (just add a 'typing' case to the router at useChatThread.ts:489), and reusing BreathingDots honors the no-spinners calm-motion doctrine. Two small implementation notes: (1) it requires a backend WS-handler change (relay + subscriber fan-out) — inherent to the feature, and CHAT-RELIABILITY-DESIGN.md already slots it as Phase B, so no doctrine conflict; (2) the indicator row should fade in/out with the existing FadeInUp/EASE tokens (DUR.default 220ms) and respect useReducedMotion so its appearance doesn't itself cause layout jump above the composer — reserve or animate the row height rather than popping it in.
**Verifier note:** Fix is minimal and fits the stack: ws.py's pump (ws.py:102-105) already forwards any broadcaster-published frame verbatim to subscribers, and mobile socket.ts already has the exact frame-send pattern to mirror (delivered/read at socket.ts:83-87 → add typing(convId)). Two small corrections to the proposal: (1) gate the server relay on `conv_id in self._pumps` (sender must be subscribed, which already proved access) instead of re-querying, and include `user_id` in the relayed frame — the pump broadcasts to ALL subscribers including the sender, so the client must drop its own typing frames; (2) render the indicator with the existing BreathingDots + FadeInUp from src/ui (no new animation), consistent with the no-spinner calm-motion doctrine. Backward compatible: old clients silently ignore the unknown 'typing' frame, as _handle does server-side.

### 23. [S3] No pull-to-refresh and no focus/foreground refetch on the daily Home / Photos / Updates tabs — stale data with no gesture to fix it
`constructo/mobile/app/(homeowner)/home.tsx:388` · ✅ verified · found by 2 auditor(s): critic, nav-screens · effort: small

**Evidence:** Home's `<ScrollView style={{ flex: 1, backgroundColor: c.bg }} contentContainerStyle={{...}}>` has no `refreshControl`; the shared `Screen` scaffold (src/ui/Screen.tsx:52-60) doesn't even accept one, so photos.tsx and updates.tsx can't offer it either. Tab screens stay mounted (bottom-tabs lazy default), react-query's `focusManager` is never wired to AppState (grep: only src/chat/useChatThread.ts listens to AppState), and only messages.tsx polls. So after the first load, Home/Photos/Updates show yesterday's data until the JS session restarts, and the user has no pull gesture to force an update
**WhatsApp gap:** WhatsApp content is always current (push-driven) and feed-style screens elsewhere universally support pull-to-refresh. Here a homeowner reopening the app sees a stale 'You're okay.' hero and old photos with literally no way to refresh short of killing the app.
**Fix:** Add an optional `refreshControl`/`onRefresh` prop to src/ui/Screen.tsx (pass through to its ScrollView) and give home.tsx a `<RefreshControl tintColor={c.accent}>` wired to `qc.invalidateQueries()`, matching what brief.tsx/approvals.tsx already do. Additionally wire `focusManager.setFocused` to AppState in app/_layout.tsx (5 lines) so react-query refetches stale queries when the app foregrounds.
**Verifier note:** Fix is minimal and matches the codebase's own pattern: brief.tsx:348-357 and approvals.tsx already wrap a ScrollView with <RefreshControl tintColor={theme.colors.accent}>, so adding an optional refreshControl passthrough to Screen.tsx and wiring home.tsx to qc.invalidateQueries() is consistent — RefreshControl with the accent tint doesn't violate the no-spinners doctrine since 4 shipped screens use it. The focusManager.setFocused ← AppState wiring in app/_layout.tsx is the standard react-query RN recipe (~5 lines). One addition worth including: the push deep-link at app/_layout.tsx:79-89 should invalidate the matching query key (['photos'] / ['home','requests'] / ['updates']) before router.push, otherwise a 'new photo' notification still opens a stale grid.

### 24. [S3] Photos feed renders every photo card at once in a ScrollView with full-resolution images — mount cost and scroll stutter grow with the album
`constructo/mobile/app/(homeowner)/photos.tsx:762` · ✅ verified · found by 2 auditor(s): critic, nav-screens · effort: medium

**Evidence:** `{visiblePhotos.map((photo) => (<StandardCard key={photo.id} ...` inside `<Screen ...>` (line 1084 — a plain ScrollView, src/ui/Screen.tsx:52): zero virtualization. Each StandardCard shows the full `photo.image_url` as a hero (line 767 → PhotoTile → BlurUpImage, which is RN `Image` — src/ui/BlurUpImage.tsx:14 `import { Animated, Image ... } from 'react-native'` — no downsampling), and the `visiblePhotos` filter/dedupe/sort chain (lines 699-702) reruns in the render body on every state change. A 60-photo site means 60 full-res JPEG decodes on tab open.
**WhatsApp gap:** WhatsApp media feeds are virtualized and thumbnail-first — opening a chat's media never decodes every original. Our Photos tab decodes the entire feed's full-size images up front: first open of a mature site visibly hangs, then scrolling stutters as offscreen cards stay mounted.
**Fix:** Render the feed with a `FlatList` (renderItem = existing StandardCard, `windowSize`/`removeClippedSubviews` defaults are enough) instead of mapping inside Screen's ScrollView, and pass `photo.thumbnail_url ?? photo.image_url` to the hero tile (full-res only in the lightbox). Wrap the visiblePhotos chain in useMemo. No new deps.
**Verifier note:** Half the fix is not implementable as written: `photo.thumbnail_url ?? photo.image_url` references a field that does not exist on the Photo type (src/api/types.ts:155-166 — thumbnail_url only exists on Update, types.ts:184), so it would be a silent no-op without a backend change. Also, dropping a FlatList inside Screen's ScrollView would nest a VirtualizedList in a plain ScrollView (kills virtualization + RN warning); the feed screen must use `<Screen scroll={false}>` with the FlatList as the scroller and the header/search/tabs as ListHeaderComponent. Right minimal fix, consistent with the stack: (1) FlatList with Screen scroll={false} + ListHeaderComponent, renderItem = existing StandardCard, default windowSize/removeClippedSubviews; (2) instead of a nonexistent thumbnail, switch BlurUpImage's inner Image to expo-image's Image (already installed per stack ground truth) — it decodes downsampled to the displayed size and disk-caches, and its transition prop can reproduce the existing settle animation, keeping the calm-motion "blur-up, never pop" doctrine; (3) useMemo on the visiblePhotos chain is harmless but optional (not the perceived cost).

### 25. [S3] A transiently-failed send sits on 'Send…' for up to 5 minutes with no tap-to-retry (only permanent 4xx failures get the retry affordance)
`constructo/mobile/src/chat/outbox.ts:12` · ✅ verified · found by 1 auditor(s): send-path · effort: small

**Evidence:** `const MAX_DELAY_MS = 5 * 60_000` (line 12) with backoff applied at line 147 `item.nextAttemptAt = Date.now() + nextAttemptDelayMs(item.attempts)`. In the screens, only `p.state === 'failed_permanent'` bubbles are wrapped in a retry Pressable — the 'queued' branch is a plain View labeled "Send…" (app/(homeowner)/messages/[id].tsx:317–328, owner chat 305–316). After a few flaky-network failures while the connection is actually back (NetInfo never flipped, e.g. captive wifi → data), the message silently waits minutes for nextAttemptAt with an unactionable "Send…" label and no way to force it.
**WhatsApp gap:** WhatsApp shows the clock, retries aggressively the moment anything changes, and lets you tap the stuck message → 'Retry now'. Here a stuck bubble is inert and the user can't tell 'sending' from 'parked for 5 minutes'.
**Fix:** Make the queued pending bubble tappable too: onPress → reset that item's nextAttemptAt to 0 (reuse retryPermanent's body for queued items) then `thread.flush()`; optionally switch the label to tapRetry once `attempts > 0`. Also cap MAX_DELAY_MS nearer 30–60s — the NetInfo/AppState drain triggers already cover long offline stretches.
**Verifier note:** Fix is minimal and already fully wired: `thread.retry(clientMsgId)` (useChatThread.ts:294-301 → retryPermanent, outbox.ts:84-92) works for 'queued' items as-is — retryPermanent just sets state='queued' + nextAttemptAt=0 and retry() then flushes, and the server send is idempotent on clientMsgId so a stray double-tap is safe. So the change is literally wrapping the queued branch in the same Pressable in the three screens (plus exposing `attempts` through pendingForThread in threadState.ts if the label should flip to tapRetry after attempts > 0). Capping MAX_DELAY_MS at 30-60s is also sound since the event-driven drain triggers cover long offline stretches. One worthwhile addition beyond the proposal: reset nextAttemptAt to 0 on the NetInfo-reconnect flush (or pass a force flag through guardedDrain) so connectivity regain retries immediately, WhatsApp-style, without requiring a tap. No new deps, no motion implications — consistent with the calm-motion doctrine (a pressable bubble, no spinner).

### 26. [S3] Bubble components aren't memoized, so any feed change (new message, tick update) re-renders every mounted row
`constructo/mobile/src/chat/MessageView.tsx:361` · ✅ verified · found by 1 auditor(s): list-perf · effort: medium

**Evidence:** `export function MessageBubble({ ... })` — no React.memo anywhere in src/chat (grep confirms zero `memo(` usages). MessageFeed's renderItem useCallback deps include `annotations` and `deliveryStateFor` (MessageFeed.tsx:247), which change on every items/cursors change — so each arriving message or receipt frame recreates renderItem and re-renders ALL mounted cells (potentially 100+ with default windowSize), not just the affected row.
**WhatsApp gap:** WhatsApp repaints only the new/changed bubble on arrival or tick upgrade; here an incoming message or read-receipt mid-scroll re-renders the whole window of bubbles on the JS thread — a felt hitch on Android during active conversations.
**Fix:** Extract the row body into a `const Row = React.memo(...)` that takes primitive/stable props (message, mine, showSender, isRunEnd, timestamp string, deliveryState value, snippet string), computed inside renderItem — so unchanged rows bail out even when renderItem's identity changes.
**Verifier note:** The memoized-Row extraction is minimal, zero new deps, and doesn't touch the calm-motion system — correct approach. One caveat for implementation: renderItem currently creates fresh closures per render (`onLongPress={() => onLongPressMessage(m)}` MessageFeed.tsx:206, plus `photoAction?.onSendToFeed`), which would defeat a naive shallow React.memo. The Row must accept the stable `message` object plus stable top-level handlers (invoking `onLongPressMessage(message)` inside the Row), or use a custom areEqual comparator that ignores function props. MessageBubble's internal useState (imgState) survives memoization fine since memo bails without unmounting.

### 27. [S3] Photo-comments composer floats a status-bar-height gap above the keyboard on iOS
`constructo/mobile/app/(homeowner)/comments.tsx:95` · ✅ verified · found by 1 auditor(s): keyboard · effort: small

**Evidence:** `<KeyboardAvoidingView style={{ flex: 1, ... paddingTop: insets.top }} behavior={...'padding'...} keyboardVerticalOffset={insets.top}>` (:92-96). This KAV is the screen root with its bottom at the window bottom, so the correct offset is 0; `insets.top` adds ~47-59px of empty space between the comment input and the keyboard — the same class of gap bug messages/[id].tsx:373 documents.
**WhatsApp gap:** WhatsApp's reply bar hugs the keyboard exactly. Here every comment typed on a feed photo (iOS) shows a visible dead band under the composer.
**Fix:** Set `keyboardVerticalOffset={0}` (paddingTop: insets.top is inside the KAV and is already part of its frame; no compensation needed).
**Verifier note:** `keyboardVerticalOffset={0}` is correct and minimal — it is the exact pattern already applied (with a doc comment) in messages/[id].tsx:374. Recommend copying that one-line explanatory comment so the fix isn't "cleaned up" back to insets.top later. No new deps, no motion changes needed.

### 28. [S3] All Screen-based forms: keyboard covers bottom fields/CTA with no way to scroll them into view (iOS)
`constructo/mobile/src/ui/Screen.tsx:52` · ✅ verified · found by 1 auditor(s): keyboard · effort: small

**Evidence:** Screen's `<ScrollView style={base} contentContainerStyle={...} keyboardShouldPersistTaps="handled">` (:52-60) has no KeyboardAvoidingView and no `automaticallyAdjustKeyboardInsets`, and every consumer form (issue.tsx:576/592 description inputs, request-photo.tsx:145 note + Send button below, requests.tsx:470/482, intake.tsx:748, login.tsx error/CTA) renders inputs + submit buttons below the keyboard line. On iOS the keyboard overlays the ScrollView without adding scrollable inset, so a focused lower field or the submit button sits under the keyboard and cannot be scrolled to — the user must di
**WhatsApp gap:** In WhatsApp any focused field (group subject, caption, search) is always kept visible above the keyboard automatically. Here 'Report an issue' / 'Request a photo' typing hides the very field being edited and the CTA on iOS.
**Fix:** Add `automaticallyAdjustKeyboardInsets` (iOS) to Screen's ScrollView — one stock RN prop, instantly fixes every form built on Screen without per-screen KAVs.
**Verifier note:** automaticallyAdjustKeyboardInsets (RN >= 0.68, iOS-only prop, no-op on Android) on Screen's ScrollView at src/ui/Screen.tsx:52 is the correct minimal fix: one stock RN prop, fixes every Screen-based form at once, no new deps, and no double-compensation risk since no KAV-wrapped screen uses the Screen scroll scaffold. System-driven keyboard inset animation also matches the calm-motion doctrine better than per-screen KAVs.

### 29. [S3] Chat photos are force-cropped into a fixed 200×150 landscape box regardless of aspect ratio
`constructo/mobile/src/chat/MessageView.tsx:466` · ✅ verified · found by 1 auditor(s): media · effort: medium

**Evidence:** `<View style={{ width: 200, height: 150, borderRadius: 8, marginBottom: 4, overflow: 'hidden' }}>` with `resizeMode="cover"` — every attachment, including the portrait photos site supervisors overwhelmingly shoot, is cropped to a small 4:3 landscape thumbnail; the top and bottom two-thirds of a portrait wall/column shot are cut off.
**WhatsApp gap:** WhatsApp sizes the bubble to the image's real aspect ratio (server-provided width/height, capped) so a portrait photo reads as a portrait photo. Here portrait construction photos are decapitated in the feed, and (combined with the dead tap) the cropped content is unreachable.
**Fix:** Have the backend include attachment_width/height in ChatMessageOut (it stores the object; PIL dims at upload are cheap), then size the box `{ width: min(280, maxBubbleWidth), aspectRatio: w/h }` clamped to 0.6–1.5 — space is still reserved before load, so no layout jump. Fallback to 200×150 when dims are absent.
**Verifier note:** Fix is minimal and doctrine-consistent: server-provided dims + aspectRatio-clamped box reserves space before load (no layout jump, matching "things settle"), falls back to 200x150 when dims absent, zero new deps. Server dims are correctly preferred over client Image.getSize, which would resize after load and cause the exact jump the doctrine forbids. Minor optional improvement while touching these lines: the file uses RN core Image (line 15) even though expo-image 3.x is installed — switching this bubble to expo-image would add disk caching and a built-in fade, but it is not required for the fix.

### 30. [S3] Full-screen photo viewer has no pinch-zoom and no open transition — pinch is a dead gesture on construction detail shots
`constructo/mobile/app/(homeowner)/photos.tsx:1379` · ✅ verified · found by 1 auditor(s): media · effort: small

**Evidence:** `<ScrollView contentContainerStyle={{ flexGrow: 1, ... }}>` wrapping `<Image source={{ uri: active.image_url }} resizeMode="contain" style={{ width: '100%', height: width, ... }}/>` — a plain fade Modal, a square letterboxed stock Image, and a ScrollView with no maximumZoomScale, so pinching to inspect tile grout / plaster finish does nothing.
**WhatsApp gap:** WhatsApp's viewer zooms from the tapped thumbnail and supports pinch/double-tap zoom. Homeowners inspecting site photos pinch instinctively — here the gesture is dead and the photo is capped at screen width.
**Fix:** Wrap the image alone in its own ScrollView with `maximumZoomScale={4} minimumZoomScale={1} bouncesZoom` (native pinch-zoom on iOS, RN's Android ScrollView also honors it since 0.63) and add a double-tap Pressable toggle. Same URL as the tile so it renders from cache instantly — no re-download.
**Verifier note:** Direction is right (no new deps, reuses cached URL) but the fix contains a factual error: `maximumZoomScale`/`minimumZoomScale`/`bouncesZoom` on ScrollView are iOS-ONLY props in React Native — the claim that "RN's Android ScrollView also honors it since 0.63" is false, so on Android (the pilot's dominant platform) pinch would remain dead. Correct minimal fix: (1) inner ScrollView with maximumZoomScale={4} for free native pinch on iOS; (2) cross-platform double-tap zoom by swapping the Image for Animated.Image and toggling a scale transform with Animated.timing using the existing motionTokens (EASE, DUR.default) — this covers Android with stock Animated and stays inside the calm-motion doctrine; (3) optionally a small PanResponder pinch for Android if double-tap-only proves insufficient. The missing open transition is fine to leave as fade for a minimal fix.

### 31. [S3] BlurUpImage has no onError path — an expired presigned URL leaves a permanently blank tile at opacity 0
`constructo/mobile/src/ui/BlurUpImage.tsx:57` · ✅ verified · found by 1 auditor(s): media · effort: small

**Evidence:** `<AnimatedImage {...rest} source={{ uri }} onLoad={onLoad} resizeMode="cover" style={[..., animatedStyle]} />` — opacity starts at 0 and only onLoad animates it up; there is no onError, no retry, and no failed state. The photos/feed presigned GETs expire (~1h), so any tile whose URL lapses while the tab stays mounted, or that fails on flaky site networks, silently stays a warm empty rectangle forever.
**WhatsApp gap:** WhatsApp shows a tap-to-retry download affordance when media fails; it never leaves an indistinguishable-from-loading blank. Here a failed grid tile and a loading grid tile look identical, with no recovery path short of killing the app.
**Fix:** Add `onError` → set a failed flag rendering a Feather 'image' glyph + tap-to-retry (re-set the source with a cache-busting remount, mirroring MessageView's imgState pattern at MessageView.tsx:403-411). Also reset `progress` to 0 when `uri` changes so a recycled tile doesn't pop.
**Verifier note:** Fix is minimal and doctrine-consistent, but apply it at the call sites (PhotoTile.tsx:146 and [area].tsx:927) by mirroring the codebase's OWN existing pattern — RefImage at [area].tsx:162-183 (failed flag → Feather 'image' glyph on AP.surfaceContainer) — rather than inventing a new one; if instead added inside BlurUpImage as a default fallback, it must still forward a caller-supplied onError so RefImage keeps working. The progress-reset-on-uri-change addition is correct and worth keeping: a refetched fresh presigned URL on a still-mounted tile currently pops in at full opacity (progress already 1), violating "settle, never pop".

### 32. [S3] expo-image is installed but unused — zero disk-cached media, so offline chat shows frozen loader glyphs where photos should be
`constructo/mobile/src/chat/MessageView.tsx:493` · ✅ verified · found by 1 auditor(s): media · effort: medium

**Evidence:** `name={imgState === 'failed' ? 'image' : 'loader'}` — offline, the offline-first cache paints bubbles instantly but every photo attachment sits behind a static (non-spinning) Feather 'loader' icon forever: stock RN `Image` (imported from 'react-native' at MessageView.tsx:15) has no controllable disk cache, and every presign rotation defeats the URL-keyed HTTP cache anyway. `grep "from 'expo-image'"` across src/ and app/ returns zero hits despite expo-image 3.x in node_modules.
**WhatsApp gap:** WhatsApp media is fully viewable offline once downloaded. Here a user in a basement/site dead zone opens the thread (text works — good) but every photo is a frozen loader glyph, which also reads as 'app is stuck' since the icon never animates.
**Fix:** Swap the attachment `Image` in MessageView (and BlurUpImage) to expo-image's `<Image cachePolicy="disk" cacheKey={attachmentKey ?? url} transition={140} />` — a stable cacheKey (the R2 attachment_key, not the rotating presigned URL) makes cache hits survive both presign rotation and app restarts, which also erases findings 1 and 3 at the root. Replace the static 'loader' glyph with the existing BreathingDots.
**Verifier note:** Right direction (expo-image is installed and the intended tool), two corrections needed: (1) attachment_key exists only on the SEND payload type (api/chat.ts:107), NOT on the received ChatMessage (:45 has only attachment_url) — cacheKey={attachmentKey ?? url} would silently fall back to the rotating presigned URL and defeat the cache; derive cacheKey client-side from the URL pathname (the stable R2 key — only the query string rotates), no backend plumbing needed. (2) The repo explicitly claims expo-image "crashes in Expo Go" (SendToFeedSheet.tsx:22) — verify/resolve that before the swap or the fix regresses dev. Also: use BreathingDots (already in src/ui, matches the no-spinner doctrine) instead of the static loader glyph, and prefer a DUR.gentle-consistent settle (matching BlurUpImage's blur-up gesture) over transition={140}.

### 33. [S3] Long-pressing a chat bubble gives zero feedback — no pressed dim, no haptic — until the sheet suddenly appears
`constructo/mobile/src/chat/MessageView.tsx:555` · ✅ verified · found by 1 auditor(s): feedback · effort: small

**Evidence:** `<Pressable onLongPress={longPress} ... style={bubbleStyle}>` (lines 555-560) — `bubbleStyle` is a static array, not a `({pressed}) => ...` function, so the bubble does not change at all during the ~500ms hold; no expo-haptics call fires on trigger (grep: only src/audio/HoldToTalk.tsx and app/(contractor)/mukadam/_voice.tsx import expo-haptics). The CaptureCard 'Show proof' toggle (lines 207-214) is likewise feedback-less.
**WhatsApp gap:** WhatsApp highlights/dims the bubble the moment your finger lands and fires a haptic when the long-press menu triggers — you feel the gesture register. Here long-press is the ONLY path to quote-reply (all roles) and to 'Send to feed' (contractor, daily), and the bubble sits inert until the sheet pops
**Fix:** Make the style a function: `style={({pressed}) => [bubbleStyle, longPress && pressed ? { opacity: 0.82 } : null]}`, and wrap the handler: `onLongPress={() => { Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium); longPress() }}` (expo-haptics is already installed). Apply the same pressed-opacity to the 'Show proof' Pressable.
**Verifier note:** Fix is minimal and on-stack (expo-haptics is installed; the style-function pattern already exists in this file). Two tweaks: use opacity 0.88, not 0.82, to match the in-file convention at lines 315/336; and drop the redundant `longPress &&` guard inside the style function since that Pressable branch only renders when longPress is truthy — i.e. style={({ pressed }) => [...bubbleStyle, pressed ? { opacity: 0.88 } : null]} plus onLongPress={() => { Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium); longPress() }}. Applying the same pressed opacity to the 'Show proof' Pressable is also correct.

### 34. [S3] Heads-up responses ('I approved this' / 'This isn't right') wait on the network with no optimistic state
`constructo/mobile/app/(homeowner)/home.tsx:285` · ✅ verified · found by 1 auditor(s): feedback · effort: small

**Evidence:** `respondM = useMutation({ mutationFn: ... respondHeadsUp ..., onSuccess: () => { void qc.invalidateQueries(...) } })` (lines 285-292) has no onMutate; the card only changes after POST + refetch. Meanwhile the buttons merely dim via `busy={respondM.isPending || flagM.isPending}` (line 675), which also disables EVERY card's buttons at once.
**WhatsApp gap:** WhatsApp-class acks (reactions, replies) apply instantly and reconcile in the background. Here the homeowner taps 'I approved this' and on a slow network the card just sits there with dimmed buttons for seconds — the exact 'did that register?' moment, on the app's highest-trust interaction.
**Fix:** Add `onMutate` that optimistically flips the responded card in the `['home','headsUp']` cache (e.g. set `respondable: false` so it renders the existing 'Your builder is on it.' line immediately), with snapshot rollback in `onError`; scope `busy` per finding_id instead of globally.
**Verifier note:** Right idea, but applied as written it breaks the third button: the backend (router.py:2554-2574) resolves the finding for 'approved' and acknowledges it for 'disputed', but 'show_more' leaves the finding open AND respondable — an unconditional onMutate flip to respondable:false would show 'Your builder is on it.' and then the buttons would pop back on refetch (a visible revert glitch, worse than the current lag). Corrected minimal fix: (1) onMutate flips respondable:false in the ['home','headsUp'] cache ONLY for action 'approved'|'disputed', with snapshot rollback in onError; for 'show_more' keep the card as-is. (2) Scope busy per card: pass `busyId={respondM.isPending ? respondM.variables?.id : undefined}` and disable/`loading` only the tapped card's buttons (Button already has an unused `loading` prop — no new UI needed). (3) Add Haptics.selectionAsync() in onRespond for instant tap ack, especially for show_more where the card intentionally doesn't change. All of this uses the existing stack (react-query cache, existing Button, expo-haptics) and is consistent with 'motion reassures, things settle' — the card settles into the acknowledged line instantly and the eventual refetch removal is the settle, not the feedback.

### 35. [S3] No offline/connecting affordance anywhere in the chat UI — stuck ticks and 'Send…' are unexplained
`constructo/mobile/src/chat/useChatThread.ts:467` · ✅ verified · found by 1 auditor(s): offline · effort: small

**Evidence:** `const netSub = NetInfo.addEventListener((s) => { if (s.isConnected) void flush() })` — the hook already listens to connectivity but exposes nothing; no chat screen (homeowner messages/[id].tsx, owner chat/[id].tsx, supervisor chat.tsx) renders any offline/connecting state. The header instead shows an always-green dot: messages/[id].tsx:472 `<View style={{ width: 7, height: 7, borderRadius: 4, backgroundColor: c.accent }} />`.
**WhatsApp gap:** WhatsApp puts 'Waiting for network' / 'Connecting…' in the chat header the moment you're offline, so a stuck clock icon is self-explanatory. Here, offline, the user sees bubbles frozen on 'Send…' and single ticks with zero explanation — it reads as the app being broken rather than the network being 
**Fix:** Expose `online: boolean` from useChatThread's existing NetInfo subscription (one `useState` in the already-present listener), and in each chat header swap the subtitle/green-dot to a muted 'Connecting…' line when `!online` — pure reuse of components already on screen, faded in with the existing FadeInUp/DUR.default motion.
**Verifier note:** Fix is minimal and stack-consistent: a single useState set inside the already-existing NetInfo listener (useChatThread.ts:467) plus a conditional subtitle swap in each chat header, reusing the on-screen Small/muted text and the existing FadeInUp + DUR.default motion — no new deps, no refactor, fits the "motion reassures, things settle" doctrine. Two small refinements: (a) NetInfo's isConnected can be null on the first event/initial fetch — treat null as online (`online = s.isConnected !== false`) so users never see a false "Connecting…" flash at screen open; (b) in the homeowner header, also gate the green dot itself (line 472) on `online` (grey/muted when offline) so the dot stops lying, not just the subtitle.

### 36. [S3] Queued messages survive an app kill but don't send on next launch until the user re-opens a chat screen
`constructo/mobile/src/chat/useChatThread.ts:465` · ✅ verified · found by 1 auditor(s): offline · effort: small

**Evidence:** `void flush() // drain on mount (recovers stuck items from a prior session)` — this is the ONLY app-start drain, and it lives inside useChatThread's effect, which runs only when a chat thread screen mounts. grep confirms no other guardedDrain/drainChatOutbox call site exists (only useChatThread.ts).
**WhatsApp gap:** Kill WhatsApp with queued messages, relaunch, leave it on the chats list — the messages send anyway. Here, if the user queued a message in a dead zone, killed the app, and next launches into Home/Updates/Photos without tapping into a thread, the message silently never sends — the recipient waits ind
**Fix:** Add one root-level drain on app start (e.g. in app/_layout.tsx): `void guardedDrain((item) => performSend(item, item.address.conversation_id ? { conversationId: item.address.conversation_id } : { siteId: item.address.site_id! }))` — both functions are already exported; items carry their own address so no thread needs to be mounted. Also arm the same NetInfo listener there.
**Verifier note:** Direction is correct and stack-consistent (no new deps, no UI/motion impact), with three adjustments: (1) add `export` to guardedDrain at useChatThread.ts:144 — it is currently module-private, contrary to the finding's claim; reusing it (rather than calling drainChatOutbox directly) matters because the module-level `draining` guard is what serializes the root drain against thread-screen flushes. (2) The per-item address reconstruction in the fix is correct — performSend uses item.address for the actual POST (useChatThread.ts:243-244) and the second arg only for presignMedia, so building ChatAddress from item.address is safe (and incidentally more correct than the mounted-thread address for cross-thread media items). (3) Gate the root drain on an authenticated session: performSend treats any 4xx as permanent (useChatThread.ts:262-264), so a drain fired before/without auth would 401 and park queued items as failed_permanent — silently converting 'will send later' into 'failed'. Arming the NetInfo listener at root alongside is good and mirrors the existing in-thread pattern.

### 37. [S3] Homeowner Home mounts through a 2-hop data waterfall, then cards pop in one query at a time (layout jump)
`constructo/mobile/app/(homeowner)/home.tsx:263` · ✅ verified · found by 1 auditor(s): nav-screens · effort: small

**Evidence:** `const milestonesQ = useQuery({ ... enabled: homeQ.isSuccess })` — milestones (263-267), requests (269-273) and headsUp (279-283) all wait for `homeQ` to finish, so cold start = 2 serial round-trips minimum. Meanwhile the whole screen is replaced by a centered `<ScreenLoader fill={false} />` (lines 300-306) — no greeting/header shell, no skeleton matching the six-card layout — and after the hero lands, the Requests card (775) and Heads-up card (671) appear seconds later, pushing the Milestones/Activity cards down.
**WhatsApp gap:** WhatsApp's home (chat list) paints instantly from local cache and never reflows as data arrives. Our daily home screen shows a breathing mark, then content that visibly re-stacks as each dependent query resolves — on a slow network you watch cards shove each other around.
**Fix:** Drop the `enabled: homeQ.isSuccess` gates (the three endpoints don't need home's result — fire all four queries in parallel), and replace the full-screen ScreenLoader with the static header + Skeleton blocks shaped like the hero/milestone cards (src/ui/Skeleton.tsx already exists) so the layout is stable before data lands. Reserve the Requests/Heads-up slots with skeletons while their queries are pending instead of inserting them later.
**Verifier note:** Fix is minimal and stack-consistent: dropping the three `enabled` gates is safe (verified the endpoints don't consume homeQ's result), and a static header + Skeleton blocks matches the existing calm doctrine better than the full-screen ScreenLoader swap. One refinement: do NOT reserve skeleton slots for the Requests/Heads-up cards — they are hidden-when-empty by design (comments at 670-674, 770-774), so a reserved skeleton that collapses to nothing when the query returns empty would itself cause the reflow the fix is trying to kill. Skeleton only the always-present cards (hero, milestones, activity); let the conditional cards keep their existing FadeInUp insertion, which is the intended settle behavior.

### 38. [S3] Doctrine-violating raw ActivityIndicator spinners on daily screens instead of Skeleton/ScreenLoader
`constructo/mobile/app/(homeowner)/updates.tsx:290` · ✅ verified · found by 1 auditor(s): nav-screens · effort: small

**Evidence:** `<ActivityIndicator color={theme.colors.accent} />` — the Updates tab's shared Loading() state. Same raw spinner on the other daily surfaces: photos.tsx:677 (feed loading card), messages/[id].tsx:512 (thread open), notifications.tsx:259, decisions/[id].tsx:263, and the contractor daily home via owner/_components.tsx:101 `LoadingBlock` (used by brief.tsx:127), plus mukadam/my-payments.tsx:120, accountant/reconcile.tsx:77. The motion doctrine ('no spinners; BreathingDots/Skeleton/ScreenLoader') is applied on Home/splash but abandoned on these screens, so loading feels like two different apps.
**WhatsApp gap:** WhatsApp essentially never shows a full-area spinner — lists render from cache and placeholders hold the layout. We flash a bare racing spinner (the exact thing the 'motion reassures, things settle' doctrine bans) on every slow-network open of Updates, Photos, a chat thread, and the contractor Brief
**Fix:** Mechanical swap: replace these ActivityIndicator loading states with the existing `ScreenLoader` (breathing mark) or, better, 2-3 `Skeleton` rows shaped like the list they precede (both already in src/ui/). Keep ActivityIndicator only for tiny in-button busy states.
**Verifier note:** Fix is right and minimal: both ScreenLoader (breathing BenchmarkMark) and Skeleton already exist in src/ui and ScreenLoader is already the established pattern on sibling screens (home.tsx, requests.tsx, inbox.tsx), so the swap is a mechanical `<ActivityIndicator/>` → `<ScreenLoader fill={false}/>` (or 2-3 Skeleton rows for list screens) with no new deps. Agree with keeping ActivityIndicator only for tiny in-button busy states (e.g. share.tsx:130, photos.tsx:1365) — those are fine as-is. The messages/[id].tsx case is lowest priority since the persisted chat cache already hides it after first open.

### 39. [S3] No optimistic bubble for photo sends — only a tiny paperclip spinner during the whole upload
`constructo/web/src/features/chat/ChatThread.tsx:361` · ✅ verified · found by 1 auditor(s): web-chat · effort: medium

**Evidence:** During upload nothing enters the thread at all (ChatComposer.processFile only flips the paperclip icon to a 20px spinner, ChatComposer.tsx:403-419); the pending entry is only created AFTER the R2 PUT completes, and even then renders as `{p.body ?? '…'}` — a text bubble containing a literal ellipsis, no thumbnail. The image then pops fully-formed into the thread when the POST echoes back.
**WhatsApp gap:** WhatsApp drops the photo into the thread instantly with a progress overlay on the image itself. Here a multi-second upload gives 'did it send?' anxiety — the only cue is a 20px spinner far from the thread.
**Fix:** In processFile, create `URL.createObjectURL(file)` and pass it through onSendMedia→sendMedia into the pending entry (`previewUrl` field on PendingMessage); render `<img src={p.previewUrl}>` in the pending-bubble branch with the same fixed-size box as MessageBubble, revoking the URL on confirm.
**Verifier note:** Right idea (objectURL preview on the pending bubble, no new deps) but as specified it only takes effect AFTER the R2 PUT, because sendMedia — which creates the pending entry — is only called post-upload (ChatComposer.tsx:272). The multi-second upload window, the actual "did it send?" gap, would still show nothing in the thread. Minimal correct version: create the pending entry at the START of processFile — generate the clientMsgId + URL.createObjectURL(file) there and insert a pending with previewUrl (small addition like an onUploadStart(cid, previewUrl) callback or hoisting the pending-insert out of sendMedia), render <img src={p.previewUrl}> in the ChatThread pending branch in the same fixed-size box MessageBubble uses, flip to 'failed' (Tap to retry) on upload error, and revoke the objectURL when the server echo merges. Same net size of change, closes the whole gap, consistent with the calm no-spinner doctrine.

### 40. [S3] Read receipts fire while the tab is hidden — blue ticks without anyone reading
`constructo/web/src/features/chat/useChatThread.ts:176` · ✅ verified · found by 1 auditor(s): web-chat · effort: small

**Evidence:** `void chatApi.read({ ...address, lastSeq: newestSeq })` runs whenever `newestSeq` changes (effect at lines 173-180) with no visibility/focus check. A contractor who leaves the chat tab open in the background auto-'reads' every incoming message via socket frames — senders see read ticks for messages nobody looked at, which also kills the unread badge that would have flagged them.
**WhatsApp gap:** WhatsApp Web only advances the read cursor when the window is focused and the chat is on screen; unread stays unread while you're away. Here the read tick lies.
**Fix:** Guard with `if (document.visibilityState === 'visible' && document.hasFocus())`; add a `visibilitychange`/`focus` listener in the same effect that flushes the deferred `chatApi.read` when the user returns.
**Verifier note:** Fix is minimal and correct for the existing stack (web-only, no motion implications, no new deps): in the same effect, gate the immediate call with `document.visibilityState === 'visible' && document.hasFocus()`; when hidden, stash newestSeq in a deferredRef instead of advancing newestSeqRef, and register `visibilitychange` + window `focus` listeners (cleaned up in the effect return) that flush the deferred chatApi.read + conversations invalidation on return. Two implementation cautions: (1) do NOT set newestSeqRef.current before the read actually fires, or the deferred flush will be skipped by the `newestSeq !== newestSeqRef.current` guard; (2) keep socket.markDelivered (line 152) ungated — delivered ticks should still advance in a background tab, matching WhatsApp.

### 41. [S3] mergeMessages hard-caps the list at 200 — 'Load older' silently discards what it just fetched
`constructo/web/src/features/chat/threadMerge.ts:10` · ✅ verified · found by 1 auditor(s): web-chat · effort: small

**Evidence:** `return sorted.slice(Math.max(0, sorted.length - max))` with `max = 200` keeps only the NEWEST 200 rows. Once a thread accumulates 200 messages, loadOlder's fetched page merges and is immediately sliced back off: the user clicks 'Load older messages' (which stays visible, since `hasOlder` is set from `older.length >= 50`) and the list doesn't change — a dead button. New incoming messages also evict the oldest visible rows while you're scrolled up in history.
**WhatsApp gap:** WhatsApp lets you scroll back through unlimited history; a 'load older' action always shows older messages. Here history beyond 200 rows is unreachable and the affordance visibly does nothing.
**Fix:** Make the cap merge-aware: in useChatThread.loadOlder call `mergeMessages(prev, older, prev.length + older.length)` (cap only applies on the append/socket path), or simply raise the default max and evict only from the older end when pinned to bottom.
**Verifier note:** The proposed fix (pass `prev.length + older.length` as max in loadOlder, cap only on the append/socket path) is minimal, pure-function-level, and stack-consistent (no new deps, no motion impact). One required addition: after history has grown past 200, the socket/send paths (useChatThread.ts:151, 200, 267, 298) still call mergeMessages with the default 200 and would instantly re-slice the grown list, yanking the just-loaded history out from under a scrolled-up reader. Those calls must pass `Math.max(200, prev.length + 1)` (or track the grown cap in a ref bumped by loadOlder). With that one-line adjustment per call site the fix is correct and complete.

### 42. [S3] Delivered cursor only advances while the thread screen is mounted — sender's ✓✓ is hours late
`constructo/mobile/app/_layout.tsx:55` · ✅ verified · found by 1 auditor(s): realtime · effort: small

**Evidence:** Only Notifications.addNotificationResponseReceivedListener (tap deep-link) exists; there is no received-listener. The delivered cursor is advanced solely by socket.markDelivered inside the thread's WS handler (useChatThread.ts:497) or implicitly by /chat/read when the thread is open. chatApi.delivered (src/api/chat.ts:311) exists but is never called anywhere.
**WhatsApp gap:** WhatsApp flips ✓ → ✓✓ within seconds of the recipient's DEVICE receiving the message, even backgrounded. Here the sender stares at a single ✓ until the recipient physically opens that thread (at which point it jumps straight to read) — 'did it reach them?' anxiety on every send to an inactive recipi
**Fix:** Add Notifications.addNotificationReceivedListener in _layout.tsx that calls chatApi.delivered({ conversationId: data.conversation_id, lastSeq: data.seq }) — the backend push payload already includes both (backend router.py:1623), and POST /chat/delivered + receipt broadcast already exist.
**Verifier note:** The fix is minimal, uses only existing pieces (expo-notifications already imported in _layout.tsx, chatApi.delivered already built, backend endpoint + receipt broadcast live), touches no motion/UI code — consistent with the stack and doctrine. Two caveats to state honestly in the PR: (a) expo-notifications' addNotificationReceivedListener fires only while the app is FOREGROUNDED; when backgrounded/killed, iOS/Android won't run the JS listener without a registered background-notification task (Notifications.registerTaskAsync + content-available), so this closes the "app open on another screen after a cold push" case but not full WhatsApp parity for backgrounded devices — acceptable as the minimal step, worth a code comment. (b) The presence hole above (foreground + socket connected + thread not subscribed → backend sends no push at all): cheap complement is to call chatApi.delivered({conversationId, lastSeq}) from the conversations-list/inbox refresh when it observes a newer last_seq, again reusing existing API with no new deps. Guard the listener with the same malformed-payload tolerance the tap handler uses (data?.conversation_id && typeof data.seq === 'number').

### 43. [S3] Each incoming socket message triggers ~3 full-thread parse/stringify cycles (mergeMessages + invalidate + resync)
`constructo/mobile/constructo/mobile/src/chat/cache.ts:33` · ✅ verified · found by 1 auditor(s): render-hygiene · effort: medium

**Evidence:** `const merged = [...bySeq.values()].sort((a, b) => a.seq - b.seq).slice(-MAX_CACHED); await AsyncStorage.setItem(KEY_PREFIX + convKey, JSON.stringify(merged))` — mergeMessages first loadThreadCache-parses the full thread (line 29), re-sorts all 2000 rows, and JSON.stringifys the whole blob to write ONE new message. The socket 'msg' handler (useChatThread.ts:494-495) then ALSO calls `qc.invalidateQueries` → the queryFn runs → maxCachedSeq (full parse) → drainPages fetch → mergeMessages again (another full parse + stringify).
**WhatsApp gap:** WhatsApp appends one row to its store when a message arrives; the transcript gains one cell. We serialize the entire history roughly three times per received message on the JS thread — in a busy crew group chat, bursts of incoming messages produce visible stutter while scrolling or typing.
**Fix:** With the in-memory cache from the previous fix, mergeMessages mutates/copies the in-memory array (append + binary-position insert by seq instead of full re-sort) and debounces the AsyncStorage.setItem persistence (e.g. 500ms trailing, plus flush on AppState background). Also skip the invalidate→refetch round-trip when the socket payload was already merged: setQueryData with the merged array directly.
**Verifier note:** Fix is minimal and stack-consistent (react-query setQueryData + AsyncStorage debounce; no new deps, no motion impact). Three notes: (a) it depends on the "previous fix" in-memory cache landing first — if that finding is dropped, add a simple module-level Map<convKey, ChatMessage[]> memo inside cache.ts instead; (b) after merging the socket payload, call qc.setQueryData(['chat','thread',addrKey], merged) directly and SKIP the invalidate — this also fixes the hidden latency bug where the bubble waits on a REST round-trip; keep the existing 8s poll + focus refetch as the gap-repair path so no resilience is lost; (c) debounced persistence is safe here because dropped tail writes are re-syncable from the server via after_seq (durable sends live in the separate outbox store) — flush via the existing AppState 'change' listener at useChatThread.ts:470 rather than a new subscription.

### 44. [S3] Every receipt frame / app-foreground re-renders all mounted bubbles even when ticks are unchanged (unconditional setCursors)
`constructo/mobile/constructo/mobile/src/chat/useChatThread.ts:403` · ✅ verified · found by 1 auditor(s): render-hygiene · effort: small

**Evidence:** `setCursors(await chatApi.cursors(addressRef.current))` — always a fresh array, no equality check. That invalidates the `deliveryStates` useMemo (line 537) → new Map → new `deliveryState` callback (line 540) → MessageFeed's renderItem dep `deliveryStateFor` changes (MessageFeed.tsx:247) → FlatList re-renders every mounted row. fetchCursors fires on every socket 'receipt' frame (line 506) and every AppState→active (line 473), and MessageBubble is not React.memo'd so each row rebuilds its full subtree.
**WhatsApp gap:** WhatsApp updates only the tick glyph of the affected messages when a read receipt lands. We repaint the entire visible transcript on every receipt/foreground event — a duplicate or no-op receipt still costs a full feed render pass.
**Fix:** Guard the setState: fetch, then `setCursors(prev => cursorsEqual(prev, next) ? prev : next)` with a cheap per-user (user_id, last_delivered_seq, last_read_seq) comparison. Returning `prev` keeps deliveryStates/deliveryState identities stable so renderItem never churns. Wrap MessageBubble in React.memo as reinforcement.
**Verifier note:** The functional-setState guard (`setCursors(prev => cursorsEqual(prev, next) ? prev : next)` comparing (user_id, last_delivered_seq, last_read_seq) per entry) is the right minimal fix: pure state hygiene, zero new deps, no motion change, and returning `prev` keeps the whole deliveryStates→deliveryState→renderItem identity chain stable so FlatList never churns on no-op receipts. One caveat on the "React.memo MessageBubble as reinforcement" part: on its own it would be largely defeated because renderItem creates fresh inline closures per render (`onLongPress={() => onLongPressMessage(m)}` at MessageFeed.tsx:206, and the per-item onSendToFeed/feedPhotoId props) — memo would need those hoisted/stabilized or a custom areEqual. The setCursors guard alone fully resolves this finding; treat the memo as optional follow-up, not required.

### 45. [S3] Photo markup freehand drawing re-renders the entire SVG on every finger move
`constructo/mobile/src/chat/PhotoMarkupCanvas.tsx:109` · 🔎 critic sweep · found by 1 auditor(s): critic · effort: small

**Evidence:** `onPanResponderMove: (e) => { ... setCurrent((cur) => { ... if (cur.tool === 'draw') return { ...cur, pts: [...cur.pts, p] } ... })` — a React setState per move event, and line 217 `<Svg>{all.map((s, i) => renderStroke(s, ...))}</Svg>` rebuilds every completed stroke plus the growing Path (pathFromPts re-stringifies all points) on each frame.
**WhatsApp gap:** WhatsApp's photo doodle line sticks to the finger with no lag. Here a long freehand stroke (or drawing after a few strokes exist) visibly trails the finger and stutters, worst on Android where JS-thread pressure is highest.
**Fix:** Split completed strokes into a `React.memo` child keyed on the `strokes` array so they never re-render mid-gesture, and batch move points in a ref flushed once per requestAnimationFrame into a single `setCurrent` — only the in-progress Path re-renders.

### 46. [S3] Hold-to-talk starts recording only after an awaited permission check + prepareToRecordAsync — first words clipped, no feedback while preparing
`constructo/mobile/src/audio/HoldToTalk.tsx:129` · 🔎 critic sweep · found by 1 auditor(s): critic · effort: small

**Evidence:** `const perm = await requestRecordingPermissionsAsync() ... await recorder.prepareToRecordAsync() ... recorder.record(); startHaptic()` — all inside the press-in path, so the mic is dead for the prepare round-trip on every hold, and the start haptic + red recording state only appear after it resolves. Users start talking on press.
**WhatsApp gap:** WhatsApp's voice note begins capturing essentially the instant you touch the mic, with immediate haptic + timer. Here the first syllables of "24 mazdoor aaye…" are cut off and the button feels dead for a beat on every recording.
**Fix:** Fire `Haptics.impactAsync` in onPressIn (line 170) before the async chain, and pre-warm the recorder: run permission + `prepareToRecordAsync()` once on mount and re-prepare after each `stop()`, so press-in only calls `recorder.record()`.

### 47. [S3] Photo comments send is fully server-round-trip: no optimistic append, no scroll-to-bottom
`constructo/mobile/app/(homeowner)/comments.tsx:74` · 🔎 critic sweep · found by 1 auditor(s): critic · effort: small

**Evidence:** `onSuccess: () => { setText(''); void qc.invalidateQueries({ queryKey: ['photo-comments', photoId] }) }` — the typed text sits in the composer until POST succeeds, then a SECOND fetch must complete before the bubble appears; the ScrollView has no ref/scrollToEnd, so on a long thread the new comment (and incoming ones) land below the fold.
**WhatsApp gap:** WhatsApp appends your message instantly with a pending tick and keeps you pinned to the newest message. Here on a slow network you tap send and stare at your own unchanged text for seconds, wondering if it went.
**Fix:** In onMutate, `qc.setQueryData` to append a local `{ id: temp, body, is_mine: true, created_at: now }` comment and clear the text immediately (rollback + restore on error, which already toasts); add a ScrollView ref with `onContentSizeChange={() => ref.current?.scrollToEnd({ animated: true })}`.

### 48. [S3] Ask assistant never auto-scrolls to the question you just sent
`constructo/mobile/app/ask.tsx:299` · 🔎 critic sweep · found by 1 auditor(s): critic · effort: small

**Evidence:** `<ScrollView style={{ flex: 1 }} contentContainerStyle={{ padding: SPACE.gutter, gap: SPACE.lg }} keyboardShouldPersistTaps="handled">` — no ref, no scrollToEnd, no onContentSizeChange. onSend (line 192) appends the pending exchange to the END of a session that is persisted across visits and stacked under the durable requests thread, so on any non-trivial history your question and the arriving answer render off-screen.
**WhatsApp gap:** In WhatsApp, sending always lands you on your own message and the reply appears in view. Here the user sends a question into an apparently unchanged screen and must manually scroll down to find both the question and the answer.
**Fix:** Add a ScrollView ref and call `scrollToEnd({ animated: true })` from `onContentSizeChange` whenever `session.length` grew (track previous length in a ref), covering both the optimistic question and the answer swap-in.

### 49. [S2] After a socket reconnect, gap backfill relies on the always-on 8s poll — sub_ok is ignored and the poll never pauses while WS is live
`constructo/mobile/src/chat/useChatThread.ts:491` · · minor (unverified) · found by 2 auditor(s): offline, realtime · effort: medium

**Evidence:** The frame handler only handles `'msg' | 'event_update' | 'receipt'` (lines 491-507) — the `sub_ok` frame the socket doc promises ("the caller knows whether to REST-backfill (sub_ok.last_seq > after_seq)", socket.ts:4) is silently dropped. Meanwhile `refetchInterval: opts?.pollMs ?? 8000` (line 342) runs unconditionally, socket up or down.
**WhatsApp gap:** WhatsApp backfills missed messages the instant the connection re-establishes (the 'Connecting…' header resolves and messages flood in immediately). Here, messages that arrived while the WS was down only appear on the next 8s poll tick — a visible up-to-8s dead period right after reconnect when the u
**Fix:** Handle `sub_ok` in the existing handler: if `frame.last_seq > await maxCachedSeq(addrKey)` invalidate the thread query (immediate backfill on reconnect). Then gate the poll on socket health: expose an `isLive` flag from ChatSocket (set in onopen/onclose) and pass `refetchInterval: isLive ? 30000 : 8000` (or false) — poll becomes the fallback it claims to be.

### 50. [S2] Owner-chat Send button has zero press feedback and no haptic anywhere in the chat send path
`constructo/mobile/app/(contractor)/owner/chat/[id].tsx:530` · · minor (unverified) · found by 1 auditor(s): send-path · effort: small

**Evidence:** The Send Pressable (lines 530–549) uses a static style object — no `({ pressed })` function, so the button gives no visual response to the finger (compare ChatComposer.tsx:152 which does `scale: pressed ? 0.96 : 1`). And no chat screen calls expo-haptics on send even though it's installed and already used for the mukadam voice button (app/(contractor)/mukadam/_voice.tsx:22 `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)`).
**WhatsApp gap:** WhatsApp's send always acknowledges the tap instantly (button animation + send sound) before any network work. Here the owner-chat tap is visually dead — combined with the sending-lock it's indistinguishable from a broken button.
**Fix:** Give the Pressable a style function applying motionTokens' PRESS_SCALE (0.97) when pressed, and add `void Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` at the top of each screen's onSend (pattern already exists in mukadam/_voice.tsx).

### 51. [S2] CaptureCard's 'Show proof' toggle fires a global LayoutAnimation that shifts the whole inverted feed
`constructo/mobile/src/chat/MessageView.tsx:154` · · minor (unverified) · found by 1 auditor(s): list-perf · effort: small

**Evidence:** `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut); setOpen((o) => !o)` — LayoutAnimation is screen-global: expanding a proof panel animates EVERY concurrent layout change that frame, so all bubbles above the card slide, and in an inverted FlatList on Android this interacts badly with virtualization (rows visibly glide/flicker as the list re-measures).
**WhatsApp gap:** WhatsApp's in-bubble expansions (e.g. read-more) animate only within the bubble; the rest of the thread stays rock-still. Here tapping 'Show proof' on a supervisor capture card ripples motion through unrelated messages.
**Fix:** Replace the global LayoutAnimation with a local stock-Animated height/opacity transition on the proof panel (Animated.timing with motionTokens EASE/DUR.default), matching the existing motion system.

### 52. [S2] Photo-preview image visibly shrinks/reflows every time the caption keyboard opens
`constructo/mobile/app/photo-preview.tsx:148` · · minor (unverified) · found by 1 auditor(s): keyboard · effort: small

**Evidence:** The photo is a `flex: 1` sibling above the KAV composer: `<Image source={{ uri }} style={{ width: '100%', height: '100%' }} resizeMode="contain" />` inside `<View style={{ flex: 1, ... }}>` (:146-150). When the KAV pads for the keyboard (iOS) or the window resizes (Android), the flex:1 container loses ~336px and the full-screen photo jumps to roughly half size, then jumps back on dismiss.
**WhatsApp gap:** WhatsApp's photo-caption screen keeps the image static and slides the caption bar up over it — the photo never re-lays-out. Here every captioned photo send has a large image jump on keyboard open and close.
**Fix:** Keep the image layer full-bleed and absolutely positioned (`StyleSheet.absoluteFill` behind the top bar and composer) so the KAV only moves the caption bar over the photo instead of squeezing the image container.

### 53. [S2] Group create/manage sheets: name/member inputs hidden behind the keyboard on iOS
`constructo/mobile/app/(contractor)/owner/_group_sheets.tsx:146` · · minor (unverified) · found by 1 auditor(s): keyboard · effort: small

**Evidence:** `<Modal visible={visible} transparent animationType="slide" ...>` with `justifyContent: 'flex-end'` (:146-148) and TextInputs at :375 and :673 inside `<ScrollView keyboardShouldPersistTaps="handled" style={{ flexGrow: 0 }}>` — no KeyboardAvoidingView in either sheet, so on iOS the keyboard overlays the bottom-anchored sheet while naming a group.
**WhatsApp gap:** WhatsApp's new-group flow keeps the subject field pinned above the keyboard. Here the field you're typing into can be entirely under the keys.
**Fix:** Same one-liner as SendToFeedSheet: wrap the sheet body in `<KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : undefined}>` inside the Modal.

### 54. [S2] Homeowner floating nav bar rides up over content on top of the Android keyboard
`constructo/mobile/src/ui/FloatingTabBar.tsx:70` · · minor (unverified) · found by 1 auditor(s): keyboard · effort: small

**Evidence:** `<View pointerEvents="box-none" style={[styles.wrap, { bottom: insets.bottom + 12 }]}>` with `wrap: { position: 'absolute', ... }` (:70, :123-127) and no Keyboard listener. On Android (default resize mode) the container shrinks, so the 64px blurred bar floats directly above the keyboard, covering the search results/content the user is typing to filter (photos.tsx:1106 and updates.tsx:1188 search inputs live on tab screens where the bar shows).
**WhatsApp gap:** WhatsApp hides its bottom nav the moment the keyboard opens (search in chats list). Here the nav pill hovers over the keyboard, eating 64px of the already-halved viewport.
**Fix:** Add the same Android `Keyboard.addListener('keyboardDidShow'/'keyboardDidHide')` visibility state as EngTabBar and return null while the keyboard is up.

### 55. [S2] Staff login OTP field has no SMS autofill (inconsistent with the other two auth screens)
`constructo/mobile/app/(auth)/login.tsx:189` · · minor (unverified) · found by 1 auditor(s): keyboard · effort: small

**Evidence:** `<TextInput value={otp} onChangeText={setOtp} keyboardType="number-pad" autoFocus maxLength={6} ...>` (:189-198) — no `textContentType="oneTimeCode"` / `autoComplete="one-time-code"`, while homeowner-login.tsx:175-176 and join.tsx:262-263 both set them.
**WhatsApp gap:** WhatsApp auto-fills/auto-reads the SMS code so you never retype it; here contractor-side staff must memorize and hand-type 6 digits on every re-login while the two sibling screens already autofill.
**Fix:** Add `textContentType="oneTimeCode"` and `autoComplete="one-time-code"` to the OTP TextInput — copy the two props from homeowner-login.tsx:175.

### 56. [S2] Cross-thread drain presigns queued media against the MOUNTED thread's address, not the item's own conversation
`constructo/mobile/src/chat/useChatThread.ts:200` · ✅ verified · found by 1 auditor(s): offline · effort: small

**Evidence:** `const presign = await chatApi.presignMedia({ ...address, kind })` — `address` is the hook's mounted-thread address (performSend(item, addressRef.current), line 289), while the message POST correctly uses `item.address` (line 244) and even the multipart fallback uses `item.address` (lines 227-229). drainChatOutbox drains ALL conversations' items, not just the mounted thread's.
**WhatsApp gap:** WhatsApp delivers a photo queued in chat A no matter which chat you open next. Here the common recovery path — photo queued offline in thread A, user later opens thread B (or app relaunches into a different thread), drain fires — presigns the upload against B's conversation/site: the object is keyed
**Fix:** One-line: `const presign = await chatApi.presignMedia({ ...(item.address.conversation_id ? { conversationId: item.address.conversation_id } : { siteId: item.address.site_id! }), kind })` — matching what the POST and multipart fallback already do. Then performSend no longer needs the second `address` argument at all.
**Verifier note:** Fix is correct and minimal: it maps snake-case item.address to presignMedia's camelCase ChatAddress union (api/chat.ts:343 checks 'conversationId' in opts), exactly mirroring what the POST and multipart fallback already do. No new deps, no motion impact. Removing performSend's now-dead second parameter is optional cleanup (touches line 289 and any tests that call performSend directly).

### 57. [S2] Updates screen's segmented tabs unmount/remount their content — scroll position lost and spinner re-flash on every switch
`constructo/mobile/app/(homeowner)/updates.tsx:277` · · minor (unverified) · found by 1 auditor(s): nav-screens · effort: small

**Evidence:** `{tab === 'timeline' ? <TimelineTab onSwitchToChanges={...} /> : null}` (277-280) — each segment is conditionally mounted, so switching Timeline → Changes → Timeline destroys and recreates the component: scroll position resets to top every time, and the first visit to each segment shows the bare `Loading()` spinner (286-292) below the header.
**WhatsApp gap:** WhatsApp's top tabs (e.g. Updates/Calls, in-chat media tabs) keep each pane's scroll state when you flick between them. Here re-checking the Timeline after peeking at Changes dumps the user back at the top.
**Fix:** Keep all four tab views mounted and toggle visibility (`<View style={{ display: tab === 'timeline' ? 'flex' : 'none', flex: 1 }}>`), or at minimum keep the previously-visited tab mounted — react-query already caches the data, so this is purely a mount-structure change.

### 58. [S2] Tab-bar and key press interactions have zero haptic feedback despite expo-haptics being installed
`constructo/mobile/src/ui/FloatingTabBar.tsx:86` · · minor (unverified) · found by 1 auditor(s): nav-screens · effort: small

**Evidence:** `const onPress = () => { const event = navigation.emit({ type: 'tabPress', ... }) ... }` — no Haptics call; grep for `expo-haptics|Haptics.` across app/ and src/ hits only mukadam/_voice.tsx and src/audio/HoldToTalk.tsx. Tab switches, the AskPill (src/ui/AskPill.tsx:40), Approve buttons, and long-press message actions all fire silently, while the tab switch itself is an instant frame-swap (visual scale only while the finger is down).
**WhatsApp gap:** WhatsApp confirms tab switches, long-press selection, and record actions with a subtle haptic tick, which is a large part of why taps feel 'registered'. Our primary nav gives no physical confirmation at all.
**Fix:** Add `void Haptics.selectionAsync()` at the top of FloatingTabBar's onPress (and impactAsync(Light) on AskPill + chat long-press menus). One import, already in package.json.

### 59. [S2] Clicking Send drops keyboard focus out of the composer
`constructo/web/src/features/chat/ChatComposer.tsx:195` · · minor (unverified) · found by 1 auditor(s): web-chat · effort: small

**Evidence:** `onSend(body); clearText()` in handleSend never refocuses the textarea, and the Send `<button>` (line 473) takes focus on click. Enter-to-send is fine, but a mouse user who clicks Send must click back into the textarea before typing the next message — the first keystrokes go to a disabled button.
**WhatsApp gap:** WhatsApp Web returns focus to the input after every send, whatever the trigger — you can immediately keep typing.
**Fix:** Add `textareaRef.current?.focus()` at the end of handleSend (and in acceptSuggestion), matching the existing pattern in completeCommand (line 178).

### 60. [S2] Inbox updates only every 15s — new messages in unopened threads feel stale
`constructo/web/src/features/chat/ChatInbox.tsx:31` · · minor (unverified) · found by 1 auditor(s): web-chat · effort: small

**Evidence:** `refetchInterval: 15_000` is the ONLY freshness mechanism for the conversation list; the WS singleton only subscribes to the currently-open thread (useChatThread.ts:145), so a message landing in any other thread takes up to 15s to bump the row/unread badge. The open thread is instant (socket), which makes the contrast noticeable.
**WhatsApp gap:** WhatsApp's chat list reorders and badges the instant any chat receives a message. Here a contractor watching the inbox at a desk waits up to 15s.
**Fix:** Cheapest extension of the existing stack: drop the interval to 5s and add `refetchOnWindowFocus: true`; better, have the socket's global frame handler (`hello`/any `msg` frame in ChatSocket._dispatch) invalidate `['chat','conversations']` via a small registered callback.

### 61. [S2] Ping has no pong timeout and no NetInfo reconnect — half-open socket lingers undetected
`constructo/mobile/src/chat/socket.ts:51` · · minor (unverified) · found by 1 auditor(s): realtime · effort: small

**Evidence:** this.pingTimer = setInterval(() => this.send({ v: 1, type: 'ping' }), ...30_000) — the server replies pong (ws.py:69) but the client never tracks it, and `send` swallows failures. After a wifi→cellular switch the socket can sit half-open for minutes: frames stop, markDelivered frames are silently dropped, and there is no NetInfo/AppState hook to force a reconnect (only the outbox flush listens to NetInfo).
**WhatsApp gap:** WhatsApp detects a dead connection within seconds of a network change and reconnects ('connecting…'); here the app silently degrades to the 8s poll with frozen ticks until TCP finally errors the socket.
**Fix:** Track lastPongAt in onmessage; in the ping interval, if no pong since the previous ping, call this.ws.close() to trigger the existing reconnect loop. In useChatThread's existing NetInfo listener (line 467), also nudge the shared socket: on isConnected, if the socket isn't OPEN, void connect().

### 62. [S2] Sender ticks freeze when the socket is down — the 8s poll never refetches cursors
`constructo/mobile/src/chat/useChatThread.ts:400` · · minor (unverified) · found by 1 auditor(s): realtime · effort: small

**Evidence:** fetchCursors is called only on mount, on AppState→active (line 473), and on a WS 'receipt' frame (line 506). The refetchInterval: 8000 poll (line 342) fetches messages only, so with the socket down (the poll's whole purpose) your ✓ never upgrades to ✓✓/read while you watch the thread.
**WhatsApp gap:** WhatsApp's ticks keep advancing on any connectivity; here on a flaky-network day the sender's ticks are stuck at 'sent' until they background/foreground the app, even though the recipient has read the message.
**Fix:** Piggyback cursors on the existing poll: call `void fetchCursors()` inside the queryFn (or in an effect keyed on q.dataUpdatedAt) so every 8s sync also refreshes ticks when receipt frames aren't flowing.

### 63. [S2] NetInfo flaps and app-foreground rebuild the whole feed data array via unconditional setOutbox
`constructo/mobile/constructo/mobile/src/chat/useChatThread.ts:283` · · minor (unverified) · found by 1 auditor(s): render-hygiene · effort: small

**Evidence:** `setOutbox(await listChatOutbox())` — refreshOutbox always sets a fresh array (even empty→empty), and flush() → refreshOutbox runs on every NetInfo event (`NetInfo.addEventListener((s) => { if (s.isConnected) void flush() })`, line 467-468) and every AppState→active. New `outbox` → `pending` useMemo recomputes (pendingForThread returns a new array, threadState.ts:66) → the screens' `items` useMemo (keyed on thread.pending) rebuilds → MessageFeed gets new data → full FlatList diff + row re-render with zero visual change.
**WhatsApp gap:** WhatsApp's transcript does not repaint when connectivity flaps; on a flaky construction-site network NetInfo can fire isConnected events every few seconds, so we repeatedly rebuild and re-render an unchanged feed exactly when the device is already struggling.
**Fix:** Same guard pattern: `setOutbox(prev => outboxEqual(prev, next) ? prev : next)` comparing clientMsgId+state pairs (both empty → keep prev). One-line helper; keeps pending/items identities stable through connectivity churn.

### 64. [S2] SettleBar home-screen fill animates width on the JS thread, right when home-load JS is busiest
`constructo/mobile/constructo/mobile/src/ui/SettleBar.tsx:77` · · minor (unverified) · found by 1 auditor(s): render-hygiene · effort: small

**Evidence:** `// width is a layout prop → cannot use the native driver.` / `useNativeDriver: false` — the 300ms grow-in animates `width: fillWidth` ('0%'→'N%'), driving a layout pass per frame on the JS thread. SettleBar renders inside StatusCard on the homeowner home, so the animation runs exactly while the home screen's queries resolve and render — the calm 'settle' motion is the first thing to stutter when JS is loaded.
**WhatsApp gap:** WhatsApp runs all its chrome motion on the UI thread so it never hitches when app JS is busy. Our signature reassurance animation is JS-driven and janks precisely at its one showing (home open, daily).
**Fix:** Render the fill at full target width inside the overflow:hidden track and animate `transform: [{ translateX }]` from -trackWidth to -(1-fill)*trackWidth with useNativeDriver:true (measure track width via onLayout). Transform-only → UI-thread animation, no per-frame layout.

### 65. [S2] Splash BenchmarkMark draw-on animation is JS-driven during app startup, when the JS thread is most congested
`constructo/mobile/constructo/mobile/src/ui/BenchmarkMark.tsx:101` · · minor (unverified) · found by 1 auditor(s): render-hygiene · effort: medium

**Evidence:** The 'build' sequence runs three Animated.timing with `useNativeDriver: false` (lines 101/108/115, stroke-draw props) over 0.5–2.2s — exactly the window when app/index.tsx is loading fonts, restoring auth, and hydrating queries on the same JS thread. The doctrine's 'motion reassures' centrepiece (Splash + ScreenLoader) is the animation most likely to visibly stutter.
**WhatsApp gap:** WhatsApp's launch transition is compositor-driven and never hitches regardless of startup work; our benchmark draw skips frames on cold start on slower Androids, reading as the very 'stall' the no-spinner doctrine exists to avoid.
**Fix:** Replace the strokeDashoffset draw with native-driver-able reveals: pre-render each stroke segment and animate opacity + scale (or a translating overflow-hidden mask View) with useNativeDriver:true; keep the existing reduced-motion instant path. The breathe loop already uses the native driver — only the build phase needs converting.

### 66. [S2] Full-screen photo viewer opens onto a blank box — full-res RN Image with no placeholder, no thumbnail handoff
`constructo/mobile/app/(homeowner)/photos.tsx:1390` · 🔎 critic sweep · found by 1 auditor(s): critic · effort: small

**Evidence:** `<Image source={{ uri: active.image_url }} resizeMode="contain" ... style={{ width: '100%', height: width, ... }} />` — a stock RN Image inside a fade Modal with no loading state or placeholder; when the OS image cache has evicted the original (common on Android/Fresco after scrolling many photos), tapping a tile fades in a dark screen with an empty square until the full-res file re-downloads.
**WhatsApp gap:** WhatsApp's viewer opens instantly showing the already-loaded thumbnail while the full image sharpens in place. Here the user taps a photo to inspect construction detail and gets a black void with no signal that anything is loading.
**Fix:** Use expo-image for the viewer with the tile's same URI as `placeholder` (shared disk cache makes the thumbnail-quality render immediate) and `transition={200}`, so the viewer always opens onto pixels.

### 67. [S1] Receipt frame triggers a REST refetch instead of applying its own payload
`constructo/mobile/src/chat/useChatThread.ts:505` · · minor (unverified) · found by 1 auditor(s): realtime · effort: small

**Evidence:** } else if (type === 'receipt') { void fetchCursors() } — the frame already carries {user_id, kind, seq} (router.py _publish_receipt), enough to advance that member's cursor locally; instead every tick update waits on a GET /chat/cursors round-trip.
**WhatsApp gap:** WhatsApp's tick flip is instant on the receipt push; here it lags by one REST round-trip (100ms–2s) after the other side reads.
**Fix:** Apply the frame optimistically: setCursors(prev => max-advance the matching user_id's last_delivered_seq/last_read_seq with frame.seq), keeping fetchCursors as a fallback for unknown user_ids.

## Refuted in verification
- ~~Send-to-feed sheet: keyboard fully covers the caption input on iOS — you type the caption blind~~ (`constructo/mobile/src/contractor/SendToFeedSheet.tsx:203`) — verifier found the claim not real as stated.

## Already at WhatsApp parity (verified handled-well)
- (send-path) Composer input clears synchronously: all three screens call setText('') BEFORE any await (e.g. homeowner [id].tsx:359, owner [id].tsx:168, supervisor chat.tsx:429), so the box empties on the tap frame — WhatsApp parity.
- (send-path) Durable-before-network send per the design doc: send()/sendMedia() enqueue to the AsyncStorage outbox first (useChatThread.ts:416-423), the bubble is storage-backed before any network, and outbox.ts:105-117 re-queues items stuck in 'sending' after an app kill — a message composed in a dead zone survives.
- (send-path) Homeowner and owner PHOTO sends are properly optimistic: the local uri is enqueued (media.localUri, homeowner [id].tsx:223-229) so the pending bubble shows the actual photo + caption immediately — no caption-only flicker, upload happens on drain.
- (send-path) Offline resilience triggers are complete: drain fires on mount, NetInfo reconnect, and AppState→active (useChatThread.ts:462-481), sends are idempotent on client_msg_id, and per-conversation FIFO order is preserved on drain (outbox.ts:118-151) so rapid sends can't reorder.
- (send-path) Permanently-failed sends never vanish: they stay in the thread as a 'Tap to retry' bubble wired to thread.retry() on every screen.
- (send-path) The inverted FlatList makes new/pending rows appear at the visual bottom by construction — no manual scrollToEnd jump on send in the kit-based screens, and pending rows are stably sorted by createdAt at the bottom (matching final placement).
- (send-path) Concurrent drain triggers are serialized (guardedDrain, useChatThread.ts:141-159) so a NetInfo-regain racing a post-send flush can't double-send.
- (send-path) Delivery ticks are cheap and correct: ✓/✓✓/read derived from member cursors (threadState.ts computeDeliveryState), pushed live via socket receipt frames, with no per-row recompute (deliveryStateMap keyed by seq).
- (list-perf) Inverted FlatList gives free stick-to-bottom on send and on arrival-when-at-bottom — no scrollToEnd hacks, no jump on your own send (contractor screen even documents this at (contractor)/owner/chat/[id].tsx:172).
- (list-perf) Pagination viewport jump is structurally avoided: older messages append at the END of the inverted data array (visual top), which doesn't shift the current offset — and drainPages loads the full thread in one sync anyway (no trickle-in).
- (list-perf) Cache-first open: the thread seeds synchronously-feeling from AsyncStorage before any network (useChatThread.ts:371-383), and syncOrCache falls back to cache on fetch failure — reopening offline shows messages, not an error flip.
- (list-perf) Durable offline outbox with idempotent client_msg_id: sends survive app-kill/dead zones, drain on mount/NetInfo-reconnect/AppState-active, pending bubbles are storage-backed, and permanent failures get an explicit tap-to-retry bubble.
- (list-perf) Pending media bubbles render the actual local photo + caption immediately (threadState.ts mediaUri) — no caption-only flicker before upload.
- (list-perf) Day separators and same-sender grouping are computed once per items change in a memoized pure helper (annotateFeed via useMemo, MessageFeed.tsx:109-139), never per row per render.
- (list-perf) keyExtractor is stable (server message id / derived deterministic keys) — no index keys.
- (list-perf) keyboardShouldPersistTaps='handled' on the list, and the homeowner KAV offset bug is fixed with an explanatory comment (no keyboard gap).
- (list-perf) The contractor owner-chat screen keeps every renderItem-feeding prop referentially stable while typing (onReply/dayLabel/replySnippetFor/photoActionFor all correctly useCallback'd) — typing there does NOT re-render the feed cells.
- (list-perf) Delivery ticks (sent/delivered/read) are derived from member cursors with a per-seq map, single WS multiplexed across threads with poll fallback, and read cursors advance automatically — WhatsApp-grade receipt plumbing.
- (list-perf) Photo-bubble failure handling: graceful placeholder instead of a blank box, and imgState re-arms on URL change so expired presigns self-heal on refresh (MessageView.tsx:403-411).
- (list-perf) Send button has a proper press state (scale 0.96 transform, ChatComposer.tsx:152) consistent with the PRESS_SCALE motion token.
- (keyboard) Homeowner thread KAV is correct and self-documenting: messages/[id].tsx:374 uses keyboardVerticalOffset={0} with a comment explaining the previously-shipped 'insets.top+56 gap bug' — the flagship chat composer sits flush on the keyboard (iOS padding / Android resize pattern is applied consistently across all three chat screens).
- (keyboard) No two-tap send anywhere in chat: MessageFeed.tsx:259 sets keyboardShouldPersistTaps="handled" (as do Screen.tsx:59 and the sheet ScrollViews), and all Send buttons live outside the scroll views, so tapping Send with the keyboard open fires immediately.
- (keyboard) Inverted FlatList + keyboard interplay is right: offset-0 anchoring means opening the keyboard keeps the newest messages pinned to the visual bottom with no scroll jump.
- (keyboard) OTP autofill + auto-verify are WhatsApp-grade on the homeowner path: homeowner-login.tsx:175-176 and join.tsx:262-263 set textContentType="oneTimeCode" + autoComplete="one-time-code", and login screens auto-submit when the 6th digit lands (no extra confirm tap), settling with the calm-verify motion instead of a spinner.
- (keyboard) photo-preview.tsx handles keyboard edges deliberately: tapping the image dismisses the keyboard (:145) and Keyboard.dismiss() runs before entering markup mode (:121) to avoid the dropped-keyboard Modal glitch; StepUpModal.tsx:121 correctly wraps its input in a KeyboardAvoidingView.
- (keyboard) FloatingTabBar.tsx:64-65 hides the homeowner nav on every pushed route (chat thread, comments, issue), so no bottom-nav chrome ever collides with a composer on iOS.
- (keyboard) All chat/composer TextInputs use fontSize 16 and multiline with sane maxHeight caps (110-120px), so the input grows WhatsApp-style without pushing the thread around unboundedly.
- (media) Chat image bubbles reserve a fixed 200×150 box BEFORE load (MessageView.tsx:466) and the CaptureCard proof image reserves height 180 — the bubble never grows or jumps when the image arrives, so there is no scroll-position shift on media load.
- (media) Optimistic photo sends show the real local photo + caption instantly: the durable outbox item carries localUri, pendingForThread exposes it as mediaUri (threadState.ts:76), and the pending bubble renders it — no caption-only text bubble flash before upload.
- (media) Presign-expiry self-healing exists in chat: imgState re-arms on URL change (MessageView.tsx:409) and the thread does a full URL-renewing refresh on mount and on every re-focus (useChatThread.ts:304-366), so photos sent >1h ago don't stay broken.
- (media) Failed chat images show an explicit broken-image state (Feather 'image' glyph over the reserved box, MessageView.tsx:476-497) instead of an infinite blank.
- (media) photo-preview.tsx is a genuinely WhatsApp-shaped confirm-before-send screen: full-screen local-URI render (instant, zero network), caption field, inline markup, pushed route instead of a Modal to avoid keyboard/navigation glitches.
- (media) PhotoTile/BlurUpImage reserve exact dimensions up front (square grid cells sized from window width, 16:9 hero) with a warm placeholder and a gentle scale-1.06→1 settle-in fade that honors Reduce Motion — no grid layout shift on load, on-doctrine motion.
- (media) The upload transport is resilient: durable AsyncStorage outbox with exponential backoff, crash recovery of stuck 'sending' items, presigned-PUT with multipart fallback, and content-type derivation that prevents the R2 signature wedge (media.ts).
- (feedback) Delivery ticks are real and live: ✓ / ✓✓ / accent-colored ✓✓ are a pure testable mapping (src/chat/tick.ts), computed from the member cursor set with min-across-others semantics (threadState.ts computeDeliveryState), and they update WITHOUT a refetch of the thread — WS 'receipt' frames trigger fetchCursors() (useChatThread.ts:505-507), plus a refresh on AppState→active. That is WhatsApp-parity tick behavior.
- (feedback) Optimistic sends are genuinely instant at the bubble level: send() resolves on outbox ENQUEUE (storage-backed, survives app-kill), the pending bubble renders immediately with the local image URI for media (no caption-only flash), shows a 'sending…' label, and permanently-failed sends get a tap-to-retry path (retryPermanent). Idempotent client_msg_id prevents dupes on retry.
- (feedback) Incoming messages double-tick fast for the other side: the WS 'msg' handler calls socket.markDelivered(conv, seq) immediately on receipt (useChatThread.ts:497), so the sender's ✓→✓✓ flip is near-instant, not poll-bound.
- (feedback) Press states are broadly present on the shared UI kit, and they are instant Pressable-state (no Animated lag): Button scale 0.98 + opacity 0.92, FloatingTabBar scale 0.96, AskPill 0.97, ListRow pressed background tint, LinkRow opacity 0.7, PhotoTile 0.98, ChatComposer send button scale 0.96 with a distinct 0.5-opacity disabled state and accessibilityState. (Note: the PRESS_SCALE=0.97 token in motionTokens.ts is never imported — values are hardcoded per component — but the felt behavior is there.)
- (feedback) The supervisor crew-chat send button is gated only on empty text (`disabled={!text.trim()}`, supervisor/chat.tsx:1100) — rapid-fire sends work there; only the homeowner + owner screens have the sending-gate defect reported above.
- (feedback) Voice capture has real haptic feedback: HoldToTalk (src/audio/HoldToTalk.tsx) and the mukadam voice screen both use expo-haptics on the press-and-hold interaction.
- (feedback) Read reporting is automatic and cheap: the read cursor POSTs whenever newestSeq advances (useChatThread.ts:387-397) and then invalidates the conversations queries so the homeowner's own unread state clears promptly after reading.
- (offline) Offline thread open is genuinely instant: the feed seeds from the persisted AsyncStorage cache on mount (useChatThread.ts:371-383) and syncOrCache (threadState.ts:18-29) falls back to cache on any fetch failure, so a reopened thread never blanks to an error while offline — WhatsApp-parity cached window.
- (offline) Durable-before-render sends: enqueueChatSend persists the message to storage BEFORE any network attempt (send/sendMedia enqueue then flush), so an app kill in a dead zone never loses a text; crash recovery re-queues items stuck in 'sending' on the next drain (outbox.ts:110-117).
- (offline) Queued media offline (homeowner + owner threads): the local URI is stored in the durable outbox item, the pending bubble renders the actual photo + caption via mediaUri, and it survives app restart; upload is a two-step with presign→PUT and a multipart fallback that self-heals signature mismatches (useChatThread.ts:186-241).
- (offline) No double-sends: client_msg_id idempotency on every POST, an app-wide serialized drain guard with a trailing re-run flag (useChatThread.ts:141-159), and per-conversation FIFO halting in drainChatOutbox preserve send order exactly like the design doc specifies.
- (offline) Permanent 4xx failures are surfaced, not silently retried forever: failed_permanent parks the item and all three chat screens render a distinct 'Tap to retry' bubble wired to thread.retry() (e.g. messages/[id].tsx:317-323, owner chat/[id].tsx:305-311, supervisor chat.tsx:344-350).
- (offline) Backoff math matches the design doc: 1s·2^n + jitter capped at 5min for the outbox (outbox.ts:55-58), and the socket has its own 1s→30s jittered exponential reconnect that resubscribes all conversations on reopen (socket.ts:107-125).
- (offline) Drain triggers cover thread mount, NetInfo connectivity regain, and AppState→active (useChatThread.ts:462-481) — foreground/reconnect recovery is wired (the gaps are the missing timer and the chat-screen-only scope, reported above).
- (nav-screens) Tab switching keeps screens mounted (bottom-tabs lazy default, no unmountOnBlur anywhere), so scroll positions on Home/Photos/Updates/Messages survive tab hops and switches are instant — WhatsApp-parity tab feel.
- (nav-screens) Cold start is genuinely calm: app/_layout.tsx:23-25 holds a warm #fbfaf7 canvas until custom fonts load (no system-font flash), and app/index.tsx plays the benchmark-build Splash exactly once per JS session with a static breathing mark (never a spinner) while auth resolves.
- (nav-screens) Press states are consistent and cheap across the kit: Button (opacity .92 + scale .98), ListRow (surface tint), LinkRow, FloatingTabBar (scale .96), AskPill (scale .97) — all via Pressable's synchronous `pressed` state, so feedback is frame-instant and inherently Reduce-Motion-safe, with ≥48px targets throughout.
- (nav-screens) The FadeInUp mount motion (src/ui/motion.tsx) runs opacity/translate on the native driver, never delays layout, and drops the rise under Reduce Motion — and it IS applied with stagger across all homeowner daily screens (home, photos, updates, requests, settings, inbox…).
- (nav-screens) Homeowner Messages inbox (app/(homeowner)/messages.tsx) is the best-behaved daily screen: RefreshControl present, 15s polling, and cached rows stay visible during background refetch (spinner only when there is truly nothing to show).
- (nav-screens) Contractor owner daily screens (brief.tsx, approvals.tsx, _chat_inbox.tsx) all have pull-to-refresh wired to react-query invalidation with themed tint.
- (nav-screens) FloatingTabBar correctly disappears on every pushed full-screen route (activeName allowlist check, FloatingTabBar.tsx:64-65) so composers/canvases are never covered, and the blur+fill translucency matches the WhatsApp-style content-scrolls-behind bar.
- (nav-screens) photo-preview.tsx is a real pushed route (not a RN Modal) specifically to avoid the modal keyboard-drop/double-send glitches — documented tradeoff that keeps the send flow stable.
- (nav-screens) Skeleton (drifting-highlight shimmer) and ScreenLoader (breathing mark) primitives exist, run on the native driver, and resolve to static frames under Reduce Motion — the pieces for skeleton-first loading are already built, they're just under-used.
- (web-chat) Optimistic text send is genuinely instant: send() pushes a pending bubble synchronously before the POST (useChatThread.ts:189), with a distinct 'sending…' state, a failed state, and a working 'Tap to retry' that reuses the same client_msg_id for idempotency (useChatThread.ts:216-244).
- (web-chat) Transport is WS-first and resilient: singleton multiplexed socket with one-time ticket auth (JWT kept out of the URL), exponential backoff + jitter reconnect (1s→30s), resubscribe-on-reconnect using the MINIMUM after_seq per conv so messages missed during the outage are replayed by the server (socket.ts:99-101, 205-210), plus 30s keepalive pings.
- (web-chat) No double bubbles on send: the REST echo and the socket frame for the same message dedupe by seq in mergeMessages, so the confirmed bubble appears exactly once (useChatThread.ts:199 comment + threadMerge.ts).
- (web-chat) Composer keyboard behavior matches WhatsApp Web: Enter sends, Shift+Enter inserts a newline, Escape dismisses the slash menu, auto-growing textarea capped at ~6 rows, and the slash menu is fully arrow-key navigable with correct aria-activedescendant wiring (ChatComposer.tsx:205-235).
- (web-chat) Thread switching keeps the shell stable — the two-pane layout, header, and composer persist; the only loading indicator is in-pane text, never a full-page spinner (ChatThread.tsx:238-244).
- (web-chat) Delivery ticks are cursor-derived (sent/delivered/read with read-tint + aria-labels, own messages only) and update promptly via 'receipt' socket frames invalidating the cursors query (useChatThread.ts:154-156, MessageBubble.tsx:27-33).
- (web-chat) Media upload path is solid mechanically: presigned direct-to-R2 PUT with multipart fallback, image/jpg→image/jpeg MIME normalization for R2 CORS, drag-and-drop with a visible drop zone, an inline error banner, and the file input cleared so the same file can be re-picked after failure (ChatComposer.tsx:241-283).
- (web-chat) Reply quoting resolves the parent from the in-memory list with a stable callback (no extra fetch, no re-render churn), and day separators are computed inline without layout thrash (ChatThread.tsx:126-131, 279-282).
- (realtime) WS push is real and wired end-to-end for open threads: server publishes 'msg' frames on every human send (router.py:821) and agent message (post_agent_message), client holds ONE multiplexed socket app-wide with ticket auth, per-conversation after_seq subscribe, and exponential-backoff+jitter reconnect (socket.ts) — no feature flag or env gate hiding it (CHAT_WS_URL derives from API_BASE, https→wss).
- (realtime) Graceful degradation: the 8s incremental after_seq poll always runs as fallback, so a dead socket costs at most ~8s of staleness in an open thread, and seq-keyed merge (cache.ts mergeMessages) means no dupes/gaps from the dual path.
- (realtime) Foreground-return freshness for the OPEN thread is handled: AppState→'active' triggers flush() (which invalidates + refetches the thread) plus fetchCursors (useChatThread.ts:470-475), and useFocusEffect forces a full after_seq=0 refresh to renew presigned media URLs on refocus.
- (realtime) event_update (AI card upgrade) frames ARE consumed live — the handler invalidates the thread query so cards upgrade without waiting for the poll (useChatThread.ts:501-504); the server publishes them via the same broadcaster.
- (realtime) Read receipts flow live in both directions while the socket is up: REST /chat/read and WS read/delivered frames both advance monotonic cursors AND publish a 'receipt' frame (router.py:_advance_cursor/_publish_receipt), with the homeowner-room delivered-only masking applied consistently on both the frame path and GET /cursors.
- (realtime) Push notifications for offline members are wired: presence-gated (no push while a socket is live), group-mute respected, 80-char preview, and the payload carries conversation_id+seq; the client registers the Expo token for all roles and deep-links a notification TAP into the correct role-specific thread route (_layout.tsx ChatPushDeepLink).
- (realtime) The cross-replica scaling seam exists: a RedisBroadcaster (chat_realtime='redis') is implemented and interface-identical to the in-memory one, so multi-worker prod WS fan-out is a config flip, not a rewrite.
- (render-hygiene) ThemeProvider context value is properly useMemo'd on [name] (ThemeProvider.tsx:33-36) — no app-wide re-render storm from the theme context.
- (render-hygiene) ToastProvider passes a stable useCallback as its context value and animates opacity with useNativeDriver:true — showing a toast doesn't re-render consumers, and the fade runs on the UI thread.
- (render-hygiene) react-query is left at default structuralSharing (app/_layout.tsx uses a bare memoized `new QueryClient()`), so an unchanged 8s poll response does NOT change the messages array identity — the feed does not re-render on a no-op poll tick (the CPU cost of producing/deep-comparing that response is the remaining problem, reported separately).
- (render-hygiene) Both contractor chat screens (owner chat [id].tsx, supervisor chat.tsx) deliberately keep MessageFeed handlers stable — onReply keyed on `thread.setReply` (a raw setState setter), dayLabel on [lang], photoActionFor on [sentToFeed] — so typing there does NOT bust renderItem; only the homeowner screen regressed.
- (render-hygiene) MessageFeed memoizes items→annotations→data derivations (annotateFeed is pure, day separators are not computed per-row), uses a stable keyExtractor, and tracks at-bottom via a ref (no setState in onScroll).
- (render-hygiene) Continuous Animated.loops (Skeleton shimmer, BreathingDots, BenchmarkMark breathe, Toast) all run with useNativeDriver:true — idle-state motion doesn't consume the JS thread.
- (render-hygiene) No console.log statements in any mobile hot path (src/chat, src/ui, src/theme, app routes).
- (render-hygiene) The chat WebSocket is a module-level singleton with a per-conversation frame-handler registry — one multiplexed socket app-wide, and its keepalive ping timer doesn't setState.
- (render-hygiene) deliveryStates/pending are memoized in useChatThread and per-row tick lookup is an O(1) Map.get, not a cursor scan per bubble.
- (render-hygiene) The initial thread paint is cache-seeded via qc.setQueryData before the network resolves (offline-first instant open), and the send path enqueues to storage first so the pending bubble does not depend on the network.