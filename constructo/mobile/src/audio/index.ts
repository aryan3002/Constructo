/**
 * On-device voice capture kit (Contractor / Neev). Real hold-to-talk audio
 * recording + the "AI proposes, human commits" transcript-confirm chip.
 */
export { HoldToTalk } from './HoldToTalk'
export type { HoldToTalkProps, RecordedAudio } from './HoldToTalk'
export { TranscriptConfirm } from './TranscriptConfirm'
export type {
  TranscriptConfirmProps,
  TranscriptConfirmStrings,
} from './TranscriptConfirm'
