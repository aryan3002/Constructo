# Backend base URL (FastAPI service). Default points at local dev backend.
VITE_API_BASE=http://localhost:8000

# When "true", the entire UI runs on in-memory mock data — no backend needed.
VITE_USE_MOCKS=true
